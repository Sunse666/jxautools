# 2026-09-23

## 开局：查进度时发现两件事（都是上轮留下的尾巴）

用户在 09-23 上午说「看下现在的进度」。查证时发现两处没落账的东西：

1. **`tools/probe_t2_score.py` 未提交**（写于 09-22 23:58，晚于当轮最后一次提交 `bfd9f3b` 的 23:53），
   而它的输出 `tools/out/t2_score_probe.txt`（gitignore 内）已经躺在那里了 —— 也就是说
   **T2-P0 的第一次实测已经跑完，但结论没进记忆、也没进任何文档**。
2. `MEMORY.md` 涨到 17320 字符，超过注入上限被截断 → 整份重写压缩到 **13806 字符**（减 20%），
   顺带把上面那条实测结论与它推翻的旧结论写进去。

## T2-P0 只读探针实测结果（`tools/probe_t2_score.py` → `tools/out/t2_score_probe.txt`）

四个问题的答案：

| # | 问题 | 结果 |
|---|---|---|
| ① | 学号能否自取 | ✅ `GetUserInfo` 的 `Xh` 拿到了（动态取，不写死在脚本里） |
| ② | 绩点接口带 `xh` 有没有用 | ❌ **带与不带逐字节同构**（都 373 字符、`Data=1` 行） |
| ③ | 教学计划（应修侧）可用吗 | ✅ `GetPersonalJxjh` 102 行 |
| ④ | 成绩单列表 | 0 行（还没生成过，属正常） |

**§6.4 的答案**：带 `xh` 也没课程组行，那一行是公式说明行（`Xh: null`、`Point: -1.0`、
`Kcmcs: "课程平均成绩点（≥2.0才可授予学位）"`）→ **不是缺参数**。按大纲口径，绩点页只能做
「口径说明 + 服务端数据」这一档。已把结论写回 `docs/T2功能实施大纲.md` §1.2 与 §6.4。

**没堵上的半个口子（必须写清）**：现有证据只排除了「`xh` 就是缺的那个参数」，
**没有区分**「该生确实没有主干课程绩点」与「还缺别的参数」。区分办法已写进 §6.4 ——
换个有成绩的高年级账号（cookie 一换即可，学号是动态取的）跑同一条探针。

**新发现的字段陷阱**：`GetPersonalJxjh` 的 `Zxf` 存在 0 值（首行 `Zxf:0` 而 `Zxs:32`）
→ 学分进度若按 `Zxf` 直接求和会**偏低**，做之前必须先定口径（`Zxs` 还是 `Zxf>0` 才计入）。
另外这条接口又是一例 **`Result=true` + `totalCount=0` 但有 102 行数据**。

## 当前进度快照

- **T1**：P0/P1 已交付；T1-P2（今日课程通知）已砍净，只留 `BootReceiver` 那件缺陷修复。
- **T2**：P0 刚开 —— 探针跑完，**代码一行没写**。大纲 §6 六条待确认里 6.4 已答，其余 5 条仍待人工登录后跑。
- **UI 改造**：UI-P0~P3 + 动效全部交付；只剩 `GradeScreen.GradeRow` 要不要 `ListItem` 化（等用户取舍）。
- **真机验收**：§1/§2/§3 共 15 项 13 过、2 项未跑（都要退登录，风险不对等）；§4、§5 全过。
- **阻塞**：课表 .ics（缺节次钟点）—— 线索指向 `ViewKebiao.aspx`（T2 §6.1，待验）。

## 本轮改动

- `.workbuddy/memory/MEMORY.md` 整份重写（压缩 20%，并更正 `GetKcPointListByXh` 的旧结论）
- `docs/T2功能实施大纲.md`：§1.2 状态行改红、§6.4 补实测答案与「半个口子」、§6 头补进度标注
- 提交 `tools/probe_t2_score.py`（上轮漏提交）

## 缺陷诊断：切页时的「字符粘连」（只诊断，未改代码）

用户报告：切到别的页面时上一页的字短暂残留，新页控件直接盖在其上，过一会儿才消失。

**根因（四环机制链，每环都有代码位置）**：
1. `AnimatedContent` 的过渡语义就是「旧内容在过渡期内**仍在组合树里、仍被绘制**」——
   项目自己的注释已写明（`Motion.kt:141-144`、`AppRoot.kt:97-101`）→ **残留窗口 = 过渡时长**
   （`Swap 170` / `SlideOut 200` / `SlideIn 260`，`Motion.kt:51-62`）。过渡一结束旧内容被移出树 → 残留消失。
   这就是「过一会儿才消失」的全部来源。
2. 两个页面根节点**都是透明的**：全应用唯一不透明底在 `MainActivity.kt:77` 的根 `Surface`，位于两层**之下**；
   页面根一律是裸 `Column(fillMaxSize())`（`GradeScreen.kt:69`、`TimetableScreen.kt:69`、`RushScreen.kt:61`、
   `LoginScreen.kt:83`）。`AppRoot.kt:110` 的 Scaffold 容器色同样在两层之下。
3. **进入层带 `fadeIn`**（`Motion.kt:121` / `Motion.kt:165-169`）→ 整棵子树（含将来的底色）整段过渡期半透明。
   **所以「给根节点加底色」单独并不够** —— 这是最容易误判的一环。
4. 盖得干净的只有自带不透明容器色的部件：顶栏 `AppBars.kt:62`、各类 `Card` → 视觉上就成了
   「新页控件盖住旧字、字还在控件之间露出来」。

**放大器（两条未实测，已在文档里标出）**：`SizeTransform(clip = false)`（`Motion.kt:232`）对整屏+半屏横移
不可见，但对小于屏幕的容器会露出来（`ProfileScreen.kt:156` 子页 Pager、`SelectionScreen.kt:112` 两半）；
嵌套过渡（`AppRoot.kt:59` → `AppRoot.kt:129` → 页内 `MotionSwap`）；掉帧把动画时钟时间映射成更长的真实时间。

**修复三档**：A 过渡层每层自带不透明底（前提，一处改全应用生效）· B1 `MotionPager` 去掉进入侧 `fadeIn`
（根治页面级）· B2 `MotionSwap` 把进入侧 alpha 压到 ~90ms 或 `EnterTransition.None` · C 容器加 `clipToBounds()`。
**核心判断：页面级切换不该用交叉淡入 —— 只要进入层 alpha 从 0 开始，重影就是必然，调时长只是缩短它。**

**这次没验的（保持诚实）**：绘制顺序按现象取用（没从字节码核）；`clip=false` 的实际可见性；掉帧的贡献。
诊断全文写进了 `docs/UI改造实施大纲.md` §10（含判据、代码位置、方案表、验证方式）。**代码一行未改**，
等用户拍板用哪一档。

## 参考基准：Pixel / Material 3 的过渡规格（已联网核数值，写进大纲 §10.7）

**最关键的一条（也是修法的依据）**：Material 是**「先出后进」，两个 alpha 窗口零重叠**。
官方 Compose 参考实现的原文数值：

```
SharedAxisX   进 = fadeIn(210, delay = 90) + slideInHorizontally(300) {  30.dp }
SharedAxisX   出 = fadeOut(90)             + slideOutHorizontally(300) { -30.dp }
FadeThrough   进 = fadeIn(210, delay = 90) + scaleIn(0.92f, 210, delay = 90)
FadeThrough   出 = fadeOut(90)
Fade          进 = fadeIn(45) + scaleIn(0.8f, 150) ｜ 出 = fadeOut(75)
```

退出 0–90ms 淡完 → 进入 90ms 才开始变亮、300ms 结束。**任何一帧只有一页在画** ——
这就是 Pixel 上看不到本缺陷的机械原因。我们现在的写法是 `fadeIn togetherWith fadeOut` 同起同止，
整个窗口都在重叠。

**三层结构要分清**（别再混着说）：系统层（窗口级，每个 Activity 独立窗口自带不透明底，
预测式返回由**手势进度**驱动、无固定时长）· 应用层（shared axis / fade through / container transform，
**Material 指定底部导航 Tab 用 fade through**）· 数值层（M3 tokens：短 50/100/150/200 ·
中 250/300/350/400 · 长 450/500/550/600；缓动 emphasized `(0.2,0,0,1)`）。

**三条修正**（比 §10.4 初版更准）：
1. B1 **不该写成「去掉 `fadeIn`」，而是「改成错开结构」**（保留 fadeIn + 90ms 延迟）——
   零重影且不丢淡入手感，数值直接抄官方。
2. `MotionPager` 位移从 **`it / 2`（半屏）改成 30dp**（固定，不随屏宽）。
3. **A（页面根补底色）从"前提"降级为"可选加固"**：错开结构下进入层变亮前旧层已 alpha=0，透不出来。

**已确认不用动的**：`MotionEasing` 三条就是 M3 的 Emphasized / Decelerate / Accelerate（`Motion.kt:81-91`）；
时长量级 170/200/260 也在 M3「中」档范围内。

**待拍板的语义差异**：Tab 切换 Material 规定用 fade through（同层级、无方向），我们用了方向性滑动。
我倾向保留滑动但按 SharedAxisX 数值改；`ProfileScreen` 首页↔子页是明确层级关系 → 标准 SharedAxisX。



---

## 切页「字符粘连」修复落地（10:00~10:25）

用户：「就按你推荐的做」→ 按 §10.7 修正版一次改完。**改动全在 `ui/Motion.kt` 一个文件，11 个调用点一行未动。**

**落地四件事**：
- `enterFadeSpec()` = `tween(210, delayMillis = 90)` / `exitFadeSpec()` = `tween(90)`，**两个入口共用一处定义**
  （各写一份的话调时长只改一处就会静默退回粘连）
- `MotionPager`：位移两侧都 300ms，距离固定 `SharedAxisOffsetDp = 30`
  （在 `MotionPager` 里先经 `LocalDensity` 换成 px —— `transitionSpec` 不是 `@Composable`，读不到 `LocalDensity`）
- 新增私有 `MotionLayer`：`Box(Modifier.background(MaterialTheme.colorScheme.background))`，两个入口内容都包它
- 两处 `AnimatedContent` 的 modifier 加 `clipToBounds()`

**三处与 §10.4 初版不同**：
1. `MotionLayer` **刻意不加 `fillMaxSize()`** —— 入口的 `modifier` 由调用点给（11 处现在都传 `fillMaxSize()`，
   但那是约定不是约束）。加了的话，哪个调用点在**有限宽度约束**下装个小卡片会被撑成满宽 = **布局**变了。
   `Box` 尺寸 = 内容尺寸，尺寸中性；页面内容本来就 `fillMaxSize()`，底照样铺满。
2. 抽两个 `*Spec()` 函数（理由见上）。
3. `MotionSwap` 进入缩放 0.98 → **0.92**（M3 fade through 值；alpha 推迟 90ms 后靠它承担"浮现感"）。

**证据**：`verify_motion.py` 20 → **31 项，31/31 PASS**（新增 §9 共 11 项）；
`probe_motion.py` **20 条变异 20 条 CAUGHT**（新增 6 条专打 §9），真实源码 md5 未变；
其余 7 个静态脚本无回归；`:app:assembleDebug` **BUILD SUCCESSFUL 零 `e:` 零 `w:`**，APK 时间戳晚于上次提交。

**顺手修掉两个工具缺陷**（都在 `verify_motion.py`）：
- `call_spans` 的参数**多带一个闭括号**（`end = j` 落在 `)` 之后）。原来的断言都是「包含判断/按键取值」，
  末尾多一个字符察觉不到；§9 第一版用**精确比较** → 误报两处「不带 delay」。改成 `end = j - 1`，加 §0f 自证。
- `strip_comments` 删块注释时**把换行也删了** → 后面所有行号前移，报出的 `Motion.kt:100` 与实际差几十行。
  改成替换成等量换行符。**「查得对但指错位置」的脚本比没有脚本更贵。**

**没验的（保持诚实）**：真机中间帧仍未取 —— 静态脚本只能证明「结构对了」，证明不了「肉眼看不出残留」。
慢速变异包（三个时长 ×8）连拍、断言「旧页独有字形与新页独有字形不同时非空」的跑法已写进 §10.8，等用户执行。

## 验收清单跟上结构变更（10:30）

改完代码回头看，**`docs/UI改造真机验收清单.md` §5 的预跑结论已经过期** —— 那 5 条是对着
**旧结构（交叉淡入 170/200/260ms）**验的，而这轮把结构整个换了。做了四件事：

- 新增 **§6 切页字符粘连修复**（5 条）：6-1 定性（看**控件之间的空隙**，不是看顶栏/卡片，那两处本来
  就盖得死）、6-2 **硬判据**（三个时长 ×8 重装，连拍中间帧，断言旧页独有字形与新页独有字形
  **不同时非空** —— 这是本次修复**唯一的直接证据**）、6-3 防误报（滑动中旧页在未覆盖区域可见是
  **滑动的定义**）、6-4 三条回归（5-2 / 5-4 / 滚动手势与 `clipToBounds` 的相容性）、6-5 crash 日志
- §5 头部与文档头都标出：**5-1 / 5-3 已失效需按 §6 重跑**，5-2 / 5-4 / 5-5 不受影响
- 顺手修掉清单里三处过期内容：`<gradle.bat>` 是**占位符**（换成真实发行版路径 —— 本仓库没有 `gradlew`）、
  §0 说「含 P1+P2+P3 的包」（补上末批 `df31f49`）、5-5 行内计数（17/17 + 14/14 → 标为"当时"，现 31/31 + 20/20）
- 原 §6「报反馈的格式」顺延为 §7

**核对方式**：§6 引用的四个常量名逐个 grep 确认在 `Motion.kt` 里存在（各 1 处）；
×8 的算术独立重算（90→720、210→1680、300→2400）；两个不变量（`exit ≤ delay`、`delay + enter == slide`）
用 Python 复算为 True；编号引用（4-3 / 5-2 / 5-4）逐个 grep 确认存在。
**写在清单里的 `654/654` 自检项数是推断不是实测**，已在原文里标明。

**一处没做的取舍**：没把 §5 的旧表删掉 —— 它是"当时验的是什么"的记录，删了就没人知道
5-1 为什么现在不算数。改成保留原表 + 顶部加失效说明。
