# T1 功能实施大纲

> 生成时间：2026-09-21
> 上游依据：`docs/教务系统可扩展功能分析.md`（实测结论）、`docs/教务系统接口清单.md`（端点清单）
> 本文只管**方案与边界**，不含代码。所有「可做」都已在真实会话上实测过。

---

## 0. 结论速览

T1 共 6 项，外加你要求的 **选课 / 抢课合并为一项**（信息架构调整）。

| # | 功能 | 数据来源 | 前置依赖 | 建议阶段 | 状态 |
|---|---|---|---|---|---|
| A | 选课 / 抢课合并为一项 | 无（纯 IA 改动） | — | **P0** | ✅ 已交付 |
| B | 考试安排页 + 考试导出 .ics | `GetKaoShiInfo_Student` ✅ | 无 | **P0** | ✅ 已交付 |
| C | 学籍信息页 | `GetUserInfo` ✅ | 无 | **P1** | ✅ 已交付 |
| D | 导师信息 + 学期规划 | `GetMyDaoshiList` / `GetMyXqPlanList` ✅ | 无 | **P1** | ✅ 已交付（分两页） |
| E | 课表导出 .ics | 本地课表 + 锚点 | ⛔ **节次时刻表**（见 §4）→ **本轮不做** | ~~P2~~ | ✖ 砍掉 |
| F | 今日课程通知 | 本地课表 + 锚点 | ✅ 仅锚点（**不需要节次时刻**，见下） | **P2** | ⬜ 待做 |
| G | 桌面小组件 | 本地课表 + 锚点 | 同上 + RemoteViews 布局 | ~~P3~~ **本轮不做** | ✖ 不做 |

> **决策（2026-09-21）**：
> 1. **作息表不写** → 依赖它的**课表 .ics 导出砍掉**（ICS 事件必须有 `DTSTART`，没有钟点就写不出）。
> 2. **今日课程通知保留** —— 它**不需要**节次钟点：通知内容只列「上午 3-4节 高等数学 · 三教 305」
>    这类块名，触发时刻是固定的每日 07:30（用户可改）。唯一依赖是**锚点**（判断今天第几周）。
> 3. 考试 / 学籍 / 导师页 → 统一挂「我的 → 我的信息」；**不加**课表页快捷入口。
> 4. 导师与学期规划**分两页**。
> 5. 桌面小组件**不做**。
> → 立项时把「今日通知依赖作息表」这个判断**改掉了**：它是被误判为受阻的，实际只依赖锚点。

**关键发现（决定阶段划分）**：教务课表数据里**只有「上午 3-4节」这种块名，没有任何钟点**
（已 grep 课表 / 教室课表 / 考试安排全部 JS，零命中）。所以：

- **考试 .ics 不依赖作息表** —— `KsSj` 自带 `晚上7：00-9：00`，可直接导出 → 放进 P0。
- **课表 .ics 依赖作息表** → 没表就写不出 `DTSTART`，砍掉。
- **今日课程提醒不需要节次钟点**（只依赖锚点）→ 保留在 P2。
  ⚠️ 上面这句原写「课表 .ics 与今日提醒必须依赖作息表」，**是错的**（提醒只需锚点），已改。

**阻塞项 1 个**：`节次 → 钟点` 表（§4）。这是唯一需要引入外部信息的地方。

---

## 1. 范围

### 做
- 底部导航 5 → 4：选课与抢课合成「选课」一项（内含抢课）
- 考试安排页（含考试 .ics 导出）
- 学籍信息页（脱敏）
- 导师信息 + 学期规划
- 课表 .ics 导出 / 日历订阅
- 今日课程通知

### 不做（本轮明确排除）
- 桌面小组件（技术上要落回 RemoteViews + XML 布局，成本与收益不成比例，建议单独一轮）
- 抢课引擎本身的行为改动（`RushEngine` / `RushPolicy` 不动，只搬 UI）
- 教务系统任何**写操作**的新增（选课/退选沿用现有，不扩）
- T2/T3 全部（教室课表、GPA、成绩单 PDF、评教、教材、消息中心…）

---

## 2. 信息架构：选课 / 抢课合并

### 现状
```
底部导航：课表 | 选课 | 抢课 | 成绩 | 我的     ← 5 项，其中两项高度耦合但被拆开
```
两者本来就是一件事的两半：`SelectionScreen` 的课程行上已经有一个「抢」按钮，点了直接进抢课队列；
`RushScreen` 又只能看到队列与引擎参数，看不到课程在哪。用户要在两个 Tab 之间来回跳。

### 调整后
```
底部导航：课表 | 选课 | 成绩 | 我的            ← 4 项
                  │
                  └─ 内层切换：〔课程 / 已选〕 〔抢课任务〕
```
- 内层用 `SecondaryTabRow`（或顶部 Segment），首层保持「课程」。
- 「课程」= 现有 `SelectionScreen`（范围切换：任选 / 必修 / 已选 + 搜索）
- 「抢课任务」= 现有 `RushScreen` 全部内容（引擎启停、节奏参数、任务列表、定时）
- 课程行上「抢」按钮 → 加入队列后**直接切到抢课视图**并给一条 `Snackbar`，不再需要用户自己找 Tab。

### 实现要点
- `AppRoot.kt` 的 `Tab` 枚举删掉 `Rush`，`Selection` 文案保持「选课」。
- `RushScreen` / `RushViewModel` **不删、不改行为**，只从「顶层 Tab」降级为「选课页的内层视图」。
  它订阅的是 `RushStore` 单例，位置变了订阅不变 —— 这一点已经天然满足，改动面很小。
- ⚠️ 内层切换状态要 `rememberSaveable`，且用**名称**而不是序号存
  （沿用 `ProfileScreen` 里 `ProfilePage.of(name)` 的既有约定，避免以后插视图读错页）。

---

## 3. 各功能设计

### 3.B 考试安排页 + 考试 .ics

**接口**：`POST /PaiKaoManage/KaoShiAnPaiChaXunManage/GetKaoShiInfo_Student/{uuid}`，body `Xq={term}`。
**现状**：接口早就在用，但**只服务于反推周次锚点，没有页面**。这是最便宜的缺口。

页面内容（按学期分组、按日期升序）：
- 每条：课程名 / 考试类别（正考·补考）/ 日期 / 时间 / 考场 / 座位或人数 / 监考老师
- `Kszhou` 为文字「未定」的行**整条跳过**（实测期末 6/16 条如此），并计入「已跳过 N 条」
- `Ksday` 是 `2026-9-11` **不补零**形态 → 用宽容解析器（项目已有）
- 空数据 `Data: []` 是**正常结果**不是失败 → 文案写「这个学期还没有安排考试」，
  **不要**写成「加载失败」。学期初只有补考是常态。

**考试 .ics 导出**（本阶段即可做，不依赖作息表）：
- 解析 `KsSj` 里的时刻，如 `2026-9-11第02周星期五晚上7：00-9：00`
  → ⚠️ **全角冒号** `：` 与「晚上 7:00」这种中文时段词都要兼容；解析不出时段时降级为**全天事件**
- 每条考试一个 `VEVENT`，`DTSTART` 用 **UTC**（`DTSTART:20260911T110000Z`）：
  中国无夏令时，用 UTC 可完全避开 `VTIMEZONE` 组件，兼容性最好
- `SUMMARY` = `【考试】课程名`，`LOCATION` = 考场，`DESCRIPTION` = 类别/监考
- `VALARM` 两条：考前 1 天、考前 1 小时

**实现记录（2026-09-21，两处与原方案的偏差 + 一处实测坑）**

1. **`UID` 必须是短 ASCII 形态**。第一版把「学期码 + 课程名」整体 `%XX` 转义进 UID，
   中文课程名一转义就有 200 字符、被折成**三行**。UID 不是给人看的，只用来去重 →
   改成 `exam-{学期码}-{课程代码}-{yyyyMMdd}-{HHmm}@jxau.tools`（如
   `exam-20261-B2211061398-20260911-1900@jxau.tools`），**只取本来就有 ASCII 形态的字段**；
   课程代码缺失才退回课程名。App 内自检 + `tools/verify_exam_ics.py` 都钉住了长度上限与形态。
2. **文件写在 `cacheDir/ics/` 下，靠 `FileProvider` 授权**，不要任何存储权限
   （Android 10+ 分区存储下也不该去要 `WRITE_EXTERNAL_STORAGE`）。
3. ⚠️ **单候选直投，多候选才套 `Intent.createChooser`**。实测（MuMu / Android 13）：
   `am start -a android.intent.action.SEND -t text/calendar` 能正常解析并打开处理者，
   但同一台设备上 `ChooserActivity` 一律报「没有应用可执行这项操作」——选择器把唯一候选吞了。
   既然 AMS 的解析能工作，就按 AMS 的口径直投；只有一个 App 能处理时本来也没有「选」的必要。
   查询候选要带 `MATCH_DEFAULT_ONLY`，且 `AndroidManifest.xml` 必须声明 `<queries>`
   （Android 11+ 包可见性过滤，不声明的话查询恒为空 → **会在真有日历 App 的真机上反过来谎报
   「没有能打开 .ics 的应用」**）。候选逐个写进 `JxauLog`，App 内诊断页直接可查。

### 3.C 学籍信息页 ✅ **已实现（2026-09-21）**

**接口**：`POST /XueJiManage/XueJiManage/GetUserInfo/{uuid}`，Referer 用 `ViewXueJiInfo` 页。
实测 **76 个字段**（原写「50+」偏保守）：学号 / 姓名 / 性别 / 民族 / 政治面貌 / 学院 / 专业 /
专业方向 / 班级 / 培养层次 / 学制 / 入学时间 / 学籍状态 / 注册状态 / 在籍状态 …

**两条硬约束**：
1. ⛔ **`Result` 不可信**：该接口返回 `"Result": false` 而 `Data` 是完整档案
   （与成绩接口 `totalCount: 0` 同类，是本项目第 4 例）。判据只能是「能否解析出数据」，
   写成 `if (Result == true)` 会把整页判成失败。
2. 🔒 **隐私**：`Sfzh` 身份证号 / `HomeAddress` 家庭住址 / `Homezip` 邮编 / `Ksh` 考生号
   → **默认遮蔽**（身份证只示后 4 位、住址只示到市），点「显示完整信息」再展开。
   不进日志、不进任何导出、不进 `.ics`。

**实现记录（2026-09-21）**

1. **字段映射用白名单，不用黑名单**。接口有 76 个字段，里面混着注册 IP、操作员、
   Excel 技术列（`ExcelRowIndex` / `HasSameDbRecord` / `IsXhValidateSuccess`…）。
   黑名单意味着**服务端以后新增一个字段就自动出现在界面上**（可能是内部字段或新的隐私字段）
   → 改成显式白名单分组，分四组（基本信息 / 学籍状态 / 培养信息 / 隐私信息），
   标签照抄校方表单，**空组不输出**。
2. **遮蔽的兜底方向必须选错得起的那个**。`Privacy.maskByKey` 对认不出的键返回
   **全遮蔽**（`"*".repeat(len)`）而不是原样返回 —— 新增一个敏感字段时宁可显示星号，
   不能默默明文。
3. **遮蔽结果本身也要能对账**。探针脚本对敏感字段额外输出 `mask=<遮蔽结果>`，
   否则对账脚本只能核对长度，构不成「界面上的星号串对不对」的证据。
4. **学籍异动失败不影响档案显示**：`XueJiYiDongList` 实测 0 行（`Data:[]`），
   它是**独立的一次请求**，失败时单独标 `changesFailed` 并在异动区块说明原因 ——
   写成「异动为空就显示没有记录」会把接口失败说成事实。
5. 档案**为空时不显示空白页**，直接报错：这个接口必然有内容，空 = 解析或会话出了问题。

### 3.D 导师信息 + 学期规划（**两个独立子页**）✅ **已实现（2026-09-21）**

**接口**：
- `POST /OneInfoManage/StudentDaoshiInfo/GetMyDaoshiList/{uuid}`（实测 2 行，30 字段）
- `POST /OneInfoManage/StudentDaoshiInfo/GetMyXqPlanList/{uuid}`（实测 2 行，32 字段）
- 子表 `GetMyBookReadList` / `GetMyZysyList`（**必须带 `Xq`**，见 `docs/教务系统接口清单.md` §4.7.4）

按 §8 决策 3 **分成两页**（导师信息 / 学期规划），都挂在「我的 → 我的信息」下。

**实现时对上一版方案的两处修正**

1. ⛔ **导师页没有职称、也没有联系方式**。上一版写「导师卡片（姓名 / 职称 / 联系方式 / 指导关系）」——
   实测 `JsBh`（教师编号）与 `JsMc`（教师名称）**恒为 `null`**，校方页面的表头也只有 10 项，
   同样没有职称与联系方式。**不是接口少传参数，是学校就没录。**
   → 这一版**不做**「职称 / 联系方式」版式，也不摆一行「职称：—」
   （那等于告诉用户 App 坏了，而事实是学校没填）。页面只展示
   导师组名 / 类型 / 成员姓名 / 状态 / 关联时间 / 学院专业班级 / 学期。
   成员姓名在 `DsTeacher` 里是**逗号分隔的一串**（`彭莹琼,廖彦文,帅欢欢,郝洁`），
   要 `splitTeachers()` 拆（半角/全角逗号、分号、顿号、空格都要认）。
2. ⚠️ **`Wysp` 是「外语水平」，不是我原先猜的「五育评分」**，而且**分数与等级混用**
   （实测 `68` 与 `良` 都有，对应类型写「其他类型」）→ 界面**不给数值加「分」字**，
   也不做数值比较。原方案里没有这一项，是探针拿全字段后才发现的。

**实现记录（其它三条）**

3. **学期规划 = 1 + 2 次请求**：主表一次拿全部学期（`GetMyXqPlanList` 返回所有学期），
   明细（书目 / 素养）按选中学期各一次。`selectTerm` 只在 `books != null && items != null`
   时才跳过重复请求 —— 只看「学期码相同」会漏掉「上次明细失败了」的情况。
4. **并发防护**：快速连点学期 chip 时晚到的响应会覆盖新学期的数据 →
   `_state.update { if (current.selected != term) return@update current ... }` 丢弃过期结果。
5. **消除一处显示噪声**：`NowStateTime` 与 `CreateTime` 实测**总是同一时刻**（差 0～270 毫秒），
   各显示一行会出现两行一模一样的日期 → 在模型层 `timeRows` 相同时合并成一行（`AdvisorRecord.timeRows`）。

### 3.E 课表 .ics 导出 / 日历订阅

**数据**：现有 `CourseSlot`，其中 `weeks: Set<Int>` 已经是**展开后的周次集合**，可以直接用。
**前置**：`TermAnchor`（第一周周一）+ 节次时刻表（§4）。

- 逐周展开成独立 `VEVENT`（**不用 `RRULE`**：周次常不连续，如 1-16 周去掉期中，
  RRULE 表达不了；逐周展开简单且绝对不会错）
- `UID` 必须**稳定且幂等**：`hash(学期 + 教学班号 + 星期 + 起始节次 + 日期)`
  —— UID 变了会导致日历里出现重复事件，这是最常见的 ICS 坑
- 跳过并计数：`weeks` 为空（周次解析失败）/ `weekday == 0` / 节次解析不出的 slot
  → 页面上显式提示「N 门课无法导出（周次或节次缺失）」，**不静默丢**
- 锚点未知时：**不导出**，提示「先做一次周次校准」并给跳转按钮（见 §5 风险 2）
- 输出：`FileProvider` + `ACTION_SEND`（`text/calendar`）分享到日历 / 邮件 / 微信；
  另给 `ACTION_VIEW` 直接交给系统日历 App

### 3.F 今日课程通知

- 每天固定时刻（默认 07:30，可改）查当天课程 → 有课则发一条汇总通知
  （课程名 / 节次-时刻 / 地点 / 老师），没课不发（可选「今天没课」）
- 依赖锚点判断「今天第几周」→ **锚点未知时不发**，并在设置项旁显示原因
  （「还不知道今天是第几周，先去校准」），不猜、不兜底成第 1 周
- 学期结束后（`todayWeek > 课表最大周次`）自动静默
- 通知点击 → 打开课表页（`PendingIntent` 到 `MainActivity` + extra 指定 Tab）
- 闹钟：复用 `RushScheduler` 的**降级模式**（无精确闹钟权限 → `setAndAllowWhileIdle`）。
  每日提醒不需要秒级精度，**默认就走非精确**，避免申请精确闹钟权限
- ⚠️ **顺手修一个既有缺陷**：`RushScheduler` 没有 `BOOT_COMPLETED` 接收器 ——
  重启后闹钟丢失，定时抢课会静默失效。本轮一并补上 `RECEIVE_BOOT_COMPLETED` + 重启重排。
  （今天课程提醒也受同一问题影响，共用这个接收器。）

### 3.G 桌面小组件（建议缓做）

技术上绕不开：Compose **不能**直接进 `RemoteViews`，只能写 XML 布局 + `AppWidgetProvider` + 手动刷新。
Glance 的依赖在本机缓存里没有（离线约束），不能引。
→ 建议本轮不做，等 P0–P2 落地后单独评估。

---

## 4. 已知缺口：节次 → 钟点表（**本轮不阻塞任何待做功能**）

> **决策**：本轮不做课表 .ics 导出，因此这个缺口不再卡住任何功能。
> 保留本节是因为它是「为什么不做课表导出」的依据，将来要做日历导出时会重新用上。

**事实**：教务系统**不提供**上课的具体时刻。课表接口只给 `Sjd`（`11`/`21` 这种拼接码）与
`Jieci` / `SjdText`（`上午 3-4节`）这类**块名**。
已 grep 全部已抓取的课表 / 教室课表 / 考试安排 JS，**零个钟点命中**；
主菜单 20 项里也没有「作息时间」入口。项目现有代码里同样没有这张表。

**但 ICS 事件必须有 `DTSTART`** —— 没有钟点就写不出日历，也写不出「还有 30 分钟上课」。

三条获取途径，按可靠性排序：

1. **学校官方作息表**（教务处公告 / 学生手册 / 校历附图）—— 最可靠，**需要你提供一次**
2. **教务系统页面兜底探测**：课表页可能有页脚作息说明；或门户首页有「作息时间」公告
   → 可以做一次定向探测（`tools/probe_pages.py`），但成功率不高，**不能当唯一方案**
3. **用户在 App 内填一次**：内置一张可编辑表，首次使用课表导出 / 提醒时引导确认

**我的建议**：方案 1 拿到真值 + 方案 3 作为兜底（用户能自己改）。
**不采用**「内置一份猜的默认值」—— 猜错的作息会让导出的日历**静默全错**，
而且用户不会意识到要去改它。宁可首次使用时明确问一次。

---

## 5. 风险与边界

| # | 风险 | 处理 |
|---|---|---|
| 1 | `Result` / `totalCount` 不可信 | 全部新接口一律按「能否解析出预期结构」判定，**不看 `Result`** |
| 2 | 锚点推不出来的同学（学期初无补考 → 考试接口 `Data: []`）→ 推不出开学日期 | 课表 .ics 与今日提醒**显式要求先校准**，宁可不做也不给错日期；文案说「校准一次即可，永久有效」 |
| 3 | 学籍页隐私（身份证 / 住址 / 邮编） | 默认遮蔽 + 不进日志 / 导出；`tools/out/` 保持 gitignore |
| 4 | 考试「未定」行、`Kszhou` 是文字 | 整条跳过 + 计数提示，不猜 |
| 5 | 考试时间文本全角冒号 | 解析器兼容全角 / 半角 / 中文时段词，解析不出降级全天事件 |
| 6 | ICS 折行按 **octet**（75 字节）且不能切断 UTF-8 多字节 | 纯逻辑自检必须有中文名跨折行边界的用例 |
| 7 | ICS 的 `UID` 不稳定 → 重复导入产生重复事件 | UID 由稳定输入派生，自检里做「两次导出 UID 集合完全相同」断言 |
| 8 | 设备重启后定时全丢 | 补 `BOOT_COMPLETED` 接收器（顺带修 `RushScheduler` 既有缺陷） |
| 9 | 通知权限（Android 13+ `POST_NOTIFICATIONS`） | 首次开启提醒时请求；拒绝时功能降级为「仅 App 内查看」并明示 |

---

## 6. 实施阶段与验收

每阶段独立可验收、独立提交（沿用项目惯例：每轮改动落一次中文提交）。

### P0 —— 导航合并 + 考试安排页（无阻塞依赖）
- 改动：`AppRoot.kt`（Tab 5→4）、`SelectionScreen.kt`（内层切换）、
  新增 `ui/exam/ExamScreen.kt` + `ExamViewModel.kt`、`JwglApi.fetchExams` 已有
- 新增：`data/export/IcsWriter.kt`（通用 ICS 写出）、`data/export/ExamIcs.kt`
- 验收：
  - 真机：底部 4 项，选课页内层可切「课程 / 抢课任务」，抢课流程与现在**完全一致**
  - 真机：考试安排页列出补考 3 条，「未定」行被跳过且计数正确
  - 导出 .ics → 用 Python `ics` 库解析回来，逐条核对日期 / 时刻 / 考场
- 可交付价值：**考试安排第一次有了页面**，考试能进手机日历

### P1 —— 学籍 / 导师 / 学期规划（无阻塞依赖）✅ **已交付（2026-09-21）**
- 新增：`JwglApi` 六个只读方法（`fetchStudentProfile` / `fetchXueJiChanges` / `fetchAdvisors` /
  `fetchTermPlans` / `fetchPlanBooks` / `fetchPlanItems`）+ `ui/student/`、`ui/advisor/`、`ui/plan/`
  三套页面 + `data/model/Profile.kt`、`data/model/AdvisorPlan.kt`
- 验收（**全部已实测通过**）：
  - 真机：学籍页 20+ 字段与教务网页逐项一致；身份证默认 `**************1117`、
    考生号 `**********1639`、住址 `江西省上饶市`、邮编 `334***`
  - 真机：`Result:false` 仍能正常渲染（**这条是最容易写错的地方，有专门用例**：
    `XueJiSchema.selfTest` 内钉住了 `Result` 与 `Data` 的组合）
  - 真机：点「显示完整」露出真值 → **返回再进自动恢复遮蔽**（`remember` 而非 `rememberSaveable`）
  - 真机：导师页 2 个学期各一条、时间行已合并；规划页学期 chip 切换后书目/素养明细跟着变
  - 隐私：**全量 logcat 对 5 个真实隐私值 0 命中**
  - 自检 **520/520**（P1 新增 57 项）+ `tools/verify_p1_pages.py` 独立对账 **54/54**

### P2 —— 今日课程通知（**只依赖锚点，不依赖作息表**）
- 新增：`service/DailyCourseAlarmReceiver.kt` + `CourseNotifier.kt`（通知渠道）+ `BootReceiver.kt`
- 设置项：「每日提醒」开关 + 触发时刻（默认 07:30），放在「我的 → 课表工具」
- 验收：
  - 自检：当天课程筛选（周次展开 + 星期匹配 + 锚点未知 / 学期结束后 / 周次解析失败）
  - 真机：把提醒时刻设到 2 分钟后，**等它真的弹出来**（不是只看日志）
  - 真机：锚点清掉后 → 提醒被拦住并说明原因，而不是发出错的课程
  - 真机：重启设备后闹钟仍在（验 `BOOT_COMPLETED`）

### ~~课表 .ics 导出~~（**本轮不做**）
缺少「节次 → 钟点」表，而 ICS 事件必须有 `DTSTART`。详见 §4。

### ~~P3 桌面小组件~~（**本轮不做**）

---

## 7. 自检与对账方式（沿用项目既有做法）

**离线纯逻辑自检**（无 JVM 单测条件，走 `kotlin-offline-pure-check` 的路子）：
- `IcsWriter`：转义（`\` `;` `,` 换行）、CRLF、**75 octet 折行不切断中文**、两次导出 UID 稳定
- `PeriodClock`：`上午 3-4节` / `下午 5-7节` / `白天 1-8节` / 脏值 → 时刻区间
- 今日课程筛选：周次展开 + 当天 + 锚点未知 / 学期结束后
- 考试时间解析：`晚上7：00-9：00`（全角）、`未定`、`2026-9-11` 不补零
- **学籍档案字段与隐私遮蔽**（`XueJiSchema.selfTest`，31 项）：白名单分组、空组不输出、
  占位值（`无`/`暂无`/`/`/`null`…）过滤、四类遮蔽各自形态、**兜底是全遮蔽不是原样**
- **学籍异动记录**（`XueJiChangeSchema.selfTest`，7 项）：原/现两组字段对照、`Admin`/`InTime`/`ID` 不取
- **导师与学期规划**（`AdvisorPlanTest.selfTest`，24 项）：`DsTeacher` 多分隔符拆分、
  `.NET` 日期解析（`/Date(-62135596800000)/` = `DateTime.MinValue` → **必须是 null**）、
  相同时间合并成一行、`XsBookCount` 与明细一致性

> ⚠️ 期望值**先用 Python 独立算出再抄进向量**。`timeRows` 那条曾经 FAIL，原因就是
> 我把两行数据的字段混搭成了一个现实中不存在的组合 —— 断言当场把它抓住了。

**真机对账**（MuMu `127.0.0.1:7555`）：
- UI 用 `uiautomator dump` 定位，截图核对
- .ics 用本机 Python 解析回来逐条比（**不看截图就说对，等于没验**）
- 通知必须**真的弹出来**才算过

---

## 8. 已定决策（2026-09-21）

| # | 决策 |
|---|---|
| 1 | **作息表不写** → 课表 .ics 导出**不做**；今日课程通知**照做**（它不需要钟点） |
| 2 | 考试 / 学籍 / 导师页统一挂 **「我的 → 我的信息」**，不加课表页快捷入口 |
| 3 | 导师信息与学期规划**分两页** |
| 4 | 桌面小组件**不做** |

### 实现时对上一版方案的修正（累计三处）

**① P0：抢课按钮不自动跳转。** 上一版写「课程行的『抢』按钮加入队列后**直接切到抢课视图**」。
实现时改成 **不自动跳转**：用户常在一屏里连点几个「抢」，
自动切走会打断这个动作。改为
①提示条上给一个「去抢课」按钮（想去再去），
②内层「抢课任务」标签上显示排队数（`抢课任务（3）`），
一眼就能看到有几个任务在等 —— 这比强制跳转更能解决「找不到任务在哪」。

**② P1：导师页没有职称、没有联系方式。** 上一版的三.C/三.D 写「导师卡片（姓名 / 职称 /
联系方式 / 指导关系）」，实测 `JsBh`/`JsMc` 恒 `null`、校方页面也没有这两项 →
**不做该版式，也不摆「职称：—」**。详见 §3.D。

**③ P1：`Wysp` 是「外语水平」不是「五育评分」**，且分数与等级两态混用 → 不加单位、不做比较。
详见 §3.D。

---

## 附：改动文件清单（**实际交付**，原为预估）

> 原表是立项时的预估，文件路径与命名实现时都变过（如 `ui/info/` 变成了 `ui/student/`+`ui/advisor/`+`ui/plan/`、
> `fetchUserInfo` 变成了 `fetchStudentProfile`）。下面按**实际提交**重写。

```
P0-A 导航合并（f1f267a）
修改
  ui/AppRoot.kt                        Tab 枚举 5 → 4
  ui/selection/SelectionScreen.kt      加内层视图切换（课程 / 抢课任务）
  ui/selection/SelectionViewModel.kt   排队数（「抢课任务（3）」）
  ui/rush/RushScreen.kt                降级为内层视图（行为零改动）

P0-B 考试安排页 + 考试 .ics（e8016b9 / 9917f03）
修改
  ui/profile/ProfileScreen.kt          新增「我的信息」分组与子页
  data/model/Academic.kt               考试条目增加时间解析
  AndroidManifest.xml                  + FileProvider + <queries>（包可见性）
  core/SelfTest.kt                     + 考试时间解析 / ICS 用例
新增
  ui/exam/ExamScreen.kt, ExamViewModel.kt
  data/export/IcsWriter.kt             通用 ICS 写出（转义 / 75 octet 折行 / CRLF）
  data/export/ExamIcs.kt               考试 → VEVENT（短 ASCII UID + VALARM 两条）
  res/xml/file_paths.xml               cacheDir/ics 授权
  tools/verify_exam_ics.py             Python 解析回来逐条对账

P1 学籍 / 导师 / 学期规划（本轮）
修改
  data/net/JwglApi.kt                  + 6 个只读方法 + JsonObject.toStringMap()
  ui/profile/ProfileScreen.kt          「我的信息」组补三行入口 + 三个子页分支
  core/SelfTest.kt                     + 3 组自检（隐私 / 异动 / 导师规划）
  docs/教务系统接口清单.md               §4.2 §4.7 §4.7.2 §4.7.4
新增
  data/model/Profile.kt                白名单分组模型 + Privacy 遮蔽 + ServerDate
  data/model/AdvisorPlan.kt            导师记录 + 学期规划 + AdvisorPlanTest
  ui/profile/DetailParts.kt            各子页共用外壳零件（SectionCard/InfoRow/…）
  ui/student/StudentScreen.kt, StudentViewModel.kt
  ui/advisor/AdvisorScreen.kt, AdvisorViewModel.kt
  ui/plan/PlanScreen.kt, PlanViewModel.kt
  tools/probe_p1_fields.py             三接口真实字段形态（隐私字段只报形态 + mask）
  tools/probe_p1_sub.py                子表参数矩阵（发现「必须带 Xq」）
  tools/verify_p1_pages.py             服务端样本 ↔ 界面文本 独立对账

P2 今日课程通知（待做）
新增
  service/DailyCourseAlarmReceiver.kt, CourseNotifier.kt, BootReceiver.kt   ← ⚠️ BootReceiver 是既有缺陷修复
  tools/verify_daily_courses.py
```
