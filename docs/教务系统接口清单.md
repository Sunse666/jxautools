# 江西农业大学教务系统 · 接口清单（实测版）

> 生成时间：2026-09-20
> 状态标记：**✅ 已实测** = 本机发请求拿到真实数据；**📖 仅来自 JS** = 从页面 JS 读到但未调用（多为写操作，刻意不试）；**❓ 未知** = 还没找到

---

## 0. 这份文档解决了什么

`D:\IO\Python\CourseQuery.py` 里只有 6 个端点，**没有课表、没有已选课程、没有考试安排**。这三个恰好是教务 App 最常用的功能。

本轮通过「登录后抓主页面 → 解析菜单 → 顺着页面 JS 挖接口 → 逐条实测」把缺口补齐，
共确认 **14 个可用端点**，其中 6 个是脚本里完全没出现过的。

---

## 1. 挖接口的方法（可复现）

不靠猜、不靠抓包，靠页面自己的 JS —— 教务系统是 ExtJS 写的，所有接口地址都明文写在页面和 JS 里。

```bash
# 1) 从 MuMu 里的调试版取出实时会话（debug 包才能 run-as）
python tools/probe_pages.py pull-cookie

# 2) 抓页面（GET，需要正确 Referer）
python tools/probe_pages.py get "/PaikeManage/KebiaoInfo/GetStudentkebiao/{uuid}"

# 3) 抓它引用的 JS，挖出 url 定义、请求参数、字段名
python tools/probe_pages.py get "Content/UIjs/PaikeManage/KebiaoInfo/GetKebiao.js"
python tools/probe_pages.py mine tools/out/pages/xxx.js

# 4) 按 JS 里的参数发请求验证
python tools/probe_pages.py post "<路径>" "<form-body>" "<该页面的 Referer>"
```

**为什么先用 Python 而不是直接在 App 里试**：安卓侧每验证一条路径都要重编 APK + 装机（约 1 分钟），
Python 改一行就发一次请求。**结论确认后再写进安卓代码**，避免把猜测固化进工程。

---

## 2. 三个必须知道的机制（不知道就一定踩坑）

### 2.1 ⚠️ 数据接口必须 POST，且必须带必需参数

数据接口（返回 JSON）**只认 POST**，并且**必需的参数一个都不能少**。违反任一条，都会返回
「您没有权限访问该页面,请与管理员联系！」——一个约 1443 字节的 HTML 错误页，而 **HTTP 状态码是 200**。
这个"200 但其实是拒绝"的坑极容易误判成调通了。

实测对照（`GetKcInfo`，同一个 URL）：

| 方法 | 参数 | 结果 |
|---|---|---|
| POST | `start=0&limit=5` | ✅ 正常 JSON |
| POST | `xklb=已选课程&start=0&limit=5` | ✅ 正常 JSON |
| POST | `dummy=1` | ❌ 1443 错误页（缺 `start`/`limit`） |
| POST | 空 body | ❌ 1443 错误页 |
| GET | `?xklb=已选课程&start=0&limit=5` | ❌ 1443 错误页（方法不对） |

所以「必需参数」指的是 `start` / `limit` 这类分页参数（以及各接口自己要求的业务参数，
如课表的 `xq`）；`xklb` 反而是可选的（不给就是查全部开课）。

> 📌 **勘误**：本轮一度判断"守卫按 Referer 授权"。后来的对照实验推翻了它——
> 同一个接口换任意 Referer（甚至不带 Referer）只要参数齐全都能通。
> 当时之所以看起来像 Referer 的问题，是因为第一次实验同时缺了 `start`/`limit`。
> 代码里仍然会带上正确 Referer（与浏览器行为一致、无害），但**它不是通不通的关键，
> 参数和方法才是**。

### 2.2 大小写不能想当然

系统里**同时存在**两套写法，不是笔误：

| 用途 | 写法 |
|---|---|
| 选课页（页面） | `KcManage/GxkcManage/XKStudentList/` ← 小写 `kc` |
| 选课接口（API） | `KcManage/GxKcManage/GetKcInfo/` ← 大写 `Kc` |

排课模块也有两套：`PaiKaoManage`（考试安排）vs `PaikeManage`（课表）。
**抄路径时逐字符核对，别靠记忆。**

### 2.3 响应封装与编码

```json
{ "Message": "", "Result": true, "Data": [...], "OtherData": null, "totalCount": 0, "success": true }
```

- 数组在 `Data`，总数在 `totalCount`
- 有的接口只有 `Result`+`Data`（如学期列表），有的带全套
- 字段名大小写混用很常见（`totalCount` 与 `TotalCount` 都出现过）
- **页面 HTML 是 GBK 编码且不带 meta charset**，按 UTF-8 硬解中文全乱（`tools/probe_pages.py` 已按「声明 → UTF-8 → GBK」顺序自适应）

### 2.4 方法：页面 GET，数据接口 POST

- **页面**（三段式路径、返回 HTML）：GET
- **数据接口**（返回 JSON）：**必须 POST**，ExtJS 的 `Ext.data.HttpProxy` 默认就是 POST

同一个路径用错方法会落到 1443 字节的错误页（HTTP 却是 200）。

---

## 3. 认证链路

完整细节见 `docs/教务系统分析与安卓App大纲.md`。这里只记与接口调用相关的部分：

```
CAS 登录 → TGT（长期，持久化）
TGT → ST（针对 jwgl 签发的一次性票据）   ← 必须用 TGT 现换，不能用登录响应里那个
ST → GET /User/CheckTicketFromSSo?ticket={ST} → 跟随跳转 → /Main/Index/{uuid}
   得到的 Cookie 才是教务系统会话
```

**兑换成功后 CookieJar 里会有三个 Cookie**：

| 名字 | 说明 |
|---|---|
| `ASP.NET_SessionId` | 真正的会话标识，**必须排在 Cookie 头最前**（脚本 `preferred_name` 逻辑） |
| `{uuid}` | 名字就是本次会话 uuid，值是 `ticket=<ST>` |
| `Password` | 值为 `Password`，无实际意义，**脚本会主动过滤掉它** |

> 📌 每次兑换 uuid 都会变（旧会话被顶掉）。所以 **uuid 必须存在会话里跟着走，不能硬编码**。

---

## 4. 接口清单

基地址：`https://jwgl.jxau.edu.cn`（WebVPN 通道需前缀重写，见第 6 节）
所有路径都带 `/{uuid}`，`{uuid}` = 会话里的 uuid。

**调用约定（否则一定失败）**
1. 数据接口一律 **POST**，form-urlencoded 请求体
2. **带全 `start` / `limit`**（分页参数是所有数据接口的必需项），再叠加业务参数
3. 带该接口所属页面的 Referer（非必需，但与浏览器一致，保留）
4. 判定成功要看**响应体是不是 JSON**，不能只看 HTTP 200

> `GET + 中文查询串` 还需要 URL 百分号编码，否则本地就抛 `ascii codec` 异常，
> 看起来像接口不通（`tools/probe_pages.py` 已处理）。

### 4.1 基础数据

| 接口 | 方法 | 参数 | 说明 | 状态 |
|---|---|---|---|---|
| `/Common/BaseData/GetKsXq/{uuid}` | POST | 无 | **学期列表**（课表/考试/成绩都用它做下拉） | ✅ |
| `/Common/BaseData/GetAllKaoShipici/{uuid}` | POST | `Xq` | 考试批次 | 📖 |
| `/Common/BaseData/GetKsXqTreeForJxrw/{uuid}` | POST | — | 学期树（按教学任务） | 📖 |
| `/User/CheckGuid/?guid={uuid}` | POST | 无 | **选课是否开放**。返回 `Result:false` 即未开放 | ✅ |

`GetKsXq` 实测返回：
```json
{"Message":null,"Result":true,"Data":[{"Key":"20261","Value":"20261","ItemID":0},{"Key":"20252",…}]}
```
学期编码规律：`YYYY` + `1`/`2`，`20261` = 2026 学年第一学期。降序排列，**第一个就是当前学期**。

### 4.2 课表（脚本里没有）

| 接口 | 方法 | 参数 | 说明 | 状态 |
|---|---|---|---|---|
| `/PaikeManage/KebiaoInfo/GetStudentkebiao/{uuid}` | GET | — | **本人课表页面**。页面里的 `var Dqxq` 就是当前学期 | ✅ |
| `/PaikeManage/KebiaoInfo/GetStudentKebiaoByXq/{uuid}` | POST | `xq` | **本人课表数据** | ✅ |
| `/PaikeManage/KebiaoInfo/GetClassroomkebiao/{uuid}` | GET | — | 按上课地点查课表（页面） | 📖 |
| `/PaikeManage/KebiaoInfo/DownloadKbinfoToExcel/{uuid}` | GET | `xq` | 导出课表 Excel | 📖 |

Referer 必须是 `https://jwgl.jxau.edu.cn/PaikeManage/KebiaoInfo/GetStudentkebiao/{uuid}`。

**接口不返回 `totalCount`**（实测顶层只有 `Message`/`Result`/`Data`/`OtherData`/`success`），
所以拿不到分页总数，一次给足 `limit`（一学期上百条封顶，`limit=1000` 够）。

**实测响应（真实数据，字段名逐字核对）**：
```json
{"Message":"","Result":true,"Data":[{
  "ID":381744,
  "KcDm":"B2211050746",        // 课程代码
  "KcMc":"Java语言程序设计",     // 课程名称
  "Jxb":"Java语言程序设计2505班", "Jxbbh":"20261132505",   // 教学班名 / 编号
  "KcXz":"课程",                // 课程性质
  "SkZhou":"17",                // 上课周次，如 "1-5,7-16"
  "Rkls":"5342.卢志群",          // 任课老师（带工号前缀）
  "Sjd":"02",  "SjdText":"星期一 上午 3-4节",   // 时间段（代码/文本）
  "XingQi":"1.星期一",  "Jieci":"上午 3-4节",
  "Skdd":"E-104",               // 上课地点
  "Jslb":"多媒体",               // 教室类别
  "Skrs":50,                    // 上课人数
  "KkDw":"软件学院", "KkDwDm":"1105",
  "Skdx":"软件2505",             // 上课对象（可能多班逗号分隔）
  "XueQi":"20261"
}]}
```

#### ⚠️ `Sjd` 不是节次序号——是一个拼接码（重要，曾经搞错过）

第一版文档写的「`Sjd` 是两位数字代码，取节次」是**错的**。实测 29 条数据的
`Sjd ↔ XingQi ↔ Jieci` 全表对照后，规律是：

```
Sjd = (星期 - 1) × 10 + 节次块序号
```

节次块序号：`1`=上午 1-2节、`2`=上午 3-4节、`3`=下午 5-6节、`4`=下午 7-8节、
`6`=下午 5-7节、`7`=晚上 9-11节、`9`=白天 1-8节。

于是 `11`/`21`/`31`/`41` **全都是「上午 1-2节」**（分别在周二/周三/周四/周五），
`02`/`12`/`22`/`42` 全是「上午 3-4节」，`09`/`19`/`29`/`39`/`49`/`59`/`69` 全是「白天 1-8节」。

**后果**：拿 `Sjd` 当序号排序，排出来的行序是错的，而且是那种「看着像对的」错。
排周课表的行必须从 `Jieci` 里解析节次区间（`TimetableGrid.parsePeriodRange`）。

#### `Jieci` 的实际取值（一学期 29 条里出现的全部 7 种）

| `Jieci` | 节次区间 | 出现次数 |
|---|---|---|
| `白天 1-8节` | 1-8（全天） | 7 |
| `上午 1-2节` | 1-2 | 5 |
| `上午 3-4节` | 3-4 | 6 |
| `下午 5-6节` | 5-6 | 4 |
| `下午 5-7节` | 5-7（三节连排） | 1 |
| `下午 7-8节` | 7-8 | 5 |
| `晚上 9-11节` | 9-11 | 1 |

注意 `下午 5-6节` 与 `下午 5-7节` 并存，以及 `白天 1-8节` 会覆盖其它所有行。
UI 侧的处理：行集合由**整学期**的 `Jieci` 归纳（不能按当前周归纳，否则切周时行会跳），
按「全天优先 → 起始节次 → 结束节次」排序。

#### `SkZhou` 的实际取值（一学期出现的 19 种）

```
1-16   1-3,6-7,9-14,16   1-4   1-4,6-7,9-17   1-5,7-16   1-5,7-17
1-7,9-13   1-7,9-17   10-11   14   16   17   19-20   2-7,9-17
3,6-7,9-13   3,6-7,9-17   3-4,6-7,9-12   3-5,7-9   4-13   4-5,7-8
```

全部是「逗号分隔 + 连字符区间」形态，没有出现单双周写法。解析器仍保留
`1-16单` / `1-16(双)` 的支持，因为学校换写法时不会通知你。

> 排课表 UI 时用 `XingQi` 取星期、`Jieci` 取节次行、`SkZhou` 判断该周是否上课。
> `SkZhou` 解析失败时必须**返回空集并在 UI 上显式提示**，不能当成「每周都上」或「都不上」。

### 4.2.1 「现在第几周」怎么算（服务端不提供，必须自己推）

服务端**没有**任何开学日期，也没有「当前周次」接口。实测排查过：
`/Common/BaseData/GetKsXq`（学期列表）只有 `Key`/`Value`；`GetKsXqTreeForJxrw`（学期树）
只有 `id`/`text`；课表页面里的 `var Dqxq` 也只是学期编码 `20261`。

**可用的锚点在考试安排里**：`Kszhou`（第几周）+ `Ksday`（该周内的具体日期）构成一个可反推的点。

实测 2026-09-20 抓到的 3 条考试安排：

| 课程 | `Kszhou` | `Ksday` | 该日星期 | 推得该周周一 | 推得第一周周一 |
|---|---|---|---|---|---|
| 线性代数A | `02` | `2026-9-11` | 五 | 2026-09-07 | 2026-08-31 |
| 高等数学D2 | `02` | `2026-9-13` | 日 | 2026-09-07 | 2026-08-31 |
| 大学物理C | `02` | `2026-9-13` | 日 | 2026-09-07 | 2026-08-31 |

3/3 一致 → 第一周周一 = **2026-08-31**。以 2026-09-20（周日）计算 → 第 3 周。

**已知的鲁棒性限制**（要写清楚，不能假装没有）：
- 学期初还没排考时**根本推不出来**。这时的行为是：周次退回第 1 周，并在界面上明说
  「没有可用的考试安排，推算不出开学日期，周次请手动确认」，**不猜**。
- 单条考试录入有误就会带偏。因此取**多数票**，并且把「N 条中 M 条一致」显示在界面上；
  不一致时标红提示「周次可能偏差一周」。
- `Ksday` 是 `2026-9-11` 这种**不补零**形态，标准 ISO 解析器吃不下（`LocalDate.parse` 会抛异常，
  Python 的 `date.fromisoformat` 同样会抛）。必须自己兜底拆 `y-M-d`。

### 4.3 选课 / 抢课（脚本里已有，这里补全）

| 接口 | 方法 | 参数 | 说明 | 状态 |
|---|---|---|---|---|
| `/KcManage/GxKcManage/GetKcInfo/{uuid}` | POST | `xklb` `Jxb` `Kkdw` `start` `limit` | 开课查询 | ✅ |
| `/KcManage/GxKcManage/GetKcInfo/{uuid}` | POST | **`xklb="已选课程"`** `start` `limit` | **已选课程查询** | ✅ |
| `/KcManage/GxKcManage/XkInfo/{uuid}` | POST | `JxbBh` `Xklb` `pcid` | 选课提交 | 📖（写操作） |
| `/KcManage/GxKcManage/DelXkinfo/{uuid}` | POST | `JxbBh` | **退选** | 📖（写操作） |
| `/KcManage/GxKcManage/GetTxInfo/{uuid}` | POST | 无 | 选课提示信息 | ✅ |
| `/KcManage/GxKcManage/Getxkqq/{uuid}` | POST | `start` `limit` | 选课开放批次（当前返回空数组 = 未开放） | ✅ |

> ### 🎯 重大发现：脚本缺的「已选课程」不用另找接口
>
> `GetKcInfo` 加一个 `xklb=已选课程` 就是已选课程列表。实测返回：
> ```json
> {"Result":true,"Data":[{"JxbBh":"20261132205","Jxb":"概率论与数理统计2205班",
>   "Kclb":"学科基础课","RkLs":"梅芳","Zxf":3.5,"Xklb":"必修","SkRs":50,"MaxRs":0,
>   "Xkrl":-50,"Sksj":"未定","Xkyq":null,"XkZt":1,"Xkpc":0,"Kkdw":"计算机与信息工程学院"}]}
> ```
> 字段：`Zxf` 总学分、`XkZt` 选课状态、`Xkrl` 选课日期、`Xkpc` 选课批次、`Xkyq` 选课要求、`MaxRs` 容量。
>
> **注意 `MaxRs` 为 0** —— 已选课程的容量字段不可靠，判断"能不能选上"要用开课查询的 `SkRs`/`MaxRs`。

Referer：`https://jwgl.jxau.edu.cn/KcManage/GxkcManage/XKStudentList/{uuid}`

> 实测：选课窗口未开放时，`GetKcInfo` 无论参数怎么给都返回「没有权限访问该页面」。
> **这意味着抢课引擎必须在窗口开放后才能在真环境验证**，本地 mock 演练是必需的。

### 4.4 成绩（脚本里已有）

| 接口 | 方法 | 参数 | 说明 | 状态 |
|---|---|---|---|---|
| `/SystemManage/CJManage/GetXsCjByXh/{uuid}` | POST | `start` `limit` | 成绩数据 | ✅ |
| `/SystemManage/PersonalScoreLookFor/PersonalScoreLookFor/{uuid}` | GET | — | 成绩单页面 | ✅ |
| `/SystemManage/PersonalScoreLookFor/GetCjPdfList/{uuid}` | POST | `start` `limit` | 已生成成绩单 PDF 列表 | 📖 |
| `/SystemManage/PersonalScoreLookFor/CreateMyCjPdf/{uuid}` | POST | — | 生成成绩单 PDF | 📖（写） |
| `/SystemManage/PersonalScoreLookFor/DownLoadMyCjPdf/{uuid}` | POST | — | 下载 PDF | 📖 |

Referer：`https://jwgl.jxau.edu.cn/SystemManage/PersonalScoreLookFor/PersonalScoreLookFor/{uuid}`

**成绩字段**：`Kcmc` 课程名、`Kcdm` 课程代码、`Xq` 学期、`Xs` 学时、`Zxf` 学分、
`Kclb` 课程类别、`Kslb` 考试类别、`Jgbj` 及格标记、`Point` 绩点（`-1.0` = 无）、
`Pscj` 平时成绩、`Kscj` 考试成绩、`Zpcj` 总评（可能是文字如 `良好`，**不一定是数字**）、
`Bkcj` 补考、`Cxcj` 重修、`Bjmc` 班级名、`Cbls` 成绩录入老师

> ⚠️ `Zpcj` 会有 `良好`/`优秀` 这类文字值，也有数字值 —— 解析必须先判类型，不能直接 `toDouble()`。

### 4.5 考试安排（脚本里没有）

| 接口 | 方法 | 参数 | 说明 | 状态 |
|---|---|---|---|---|
| `/PaiKaoManage/KaoShiAnPaiChaXunManage/Ksapcx_Student/{uuid}` | GET | — | 学生考试安排页面 | ✅ |
| `/PaiKaoManage/KaoShiAnPaiChaXunManage/GetKaoShiInfo_Student/{uuid}` | POST | `Xq` `start` `limit` | **考试安排数据** | ✅ |

Referer：`https://jwgl.jxau.edu.cn/PaiKaoManage/KaoShiAnPaiChaXunManage/Ksapcx_Student/{uuid}`

**字段**：`Ksbname` 考场名、`KsSj` 完整考试时间（`2026-9-11第02周星期五晚上7：00-9：00`）、
`Ksday` 日期、`Kswzhou`/`Kszhou` 周次、`Kstime` 节次、`Ksdd` 考试地点、`Kslb` 考试类别（`补考`/`正考`）、
`Khfs` 考核方式、`Kcmc`/`Kcdm` 课程、`Ksrs` 考试人数、`Apzt` 安排状态、`Jkls` 监考老师

### 4.6 教学计划 / 学籍（部分脚本已有）

| 接口 | 方法 | 参数 | 说明 | 状态 |
|---|---|---|---|---|
| `/Jxjh/JxjhManage/GetPersonalJxjh/{uuid}` | POST | — | 个人教学计划 | ✅（脚本已有） |
| `/Jxjh/JxjhManage/PersonalJxjh/{uuid}` | GET | — | 教学计划页面 | ✅ |
| `/XueJiManage/XueJiManage/ViewXueJiInfo/{uuid}` | GET | — | 学籍信息页面 | 📖 |
| `/OneInfoManage/StudentDaoshiInfo/MyDaoshiInfo/{uuid}` | GET | — | 导师信息 | 📖 |

### 4.7 会话维护

| 接口 | 方法 | 参数 | 说明 | 状态 |
|---|---|---|---|---|
| `/Main/Index/{uuid}` | GET | — | 主页面 + 保活 + 会话校验 | ✅ |
| `/User/Exit/?guid={uuid}` | GET | — | 退出登录 | 📖 |

---

## 5. 页面路由表（菜单，登录后从主页面 HTML 提取）

主页面 ≈ 19800 字符，菜单由 `addTab('标题', '路径')` 定义。全部路径都带 `/{uuid}`。

| 菜单 | 路径 |
|---|---|
| 本人课表查询 | `PaikeManage/KebiaoInfo/GetStudentkebiao/` |
| 按上课地点查询 | `PaikeManage/KebiaoInfo/GetClassroomkebiao/` |
| 我的考试安排 | `PaiKaoManage/KaoShiAnPaiChaXunManage/Ksapcx_Student/` |
| 我的成绩单 | `SystemManage/PersonalScoreLookFor/PersonalScoreLookFor/` |
| 我的教学计划 | `Jxjh/JxjhManage/PersonalJxjh/` |
| 查看教学计划 | `Jxjh/JxjhManage/JxjhManage/` |
| 网上选课 | `KcManage/GxkcManage/XKStudentList/` |
| 我的学籍信息 | `XueJiManage/XueJiManage/ViewXueJiInfo/` |
| 查看专业信息 | `XueJiManage/XueJiManage/ProfessionalManage/` |
| 核查毕业存根 | `XueJiManage/WenPinCunGen/hcbycg/` |
| 核对教材信息 | `JcManage/JcLqXsManage/JcLqXsManage/` |
| 等级考试报名 | `KjManage/KjBmManage/XsBm/` |
| 免修课程申请 | `CJManage/MXKCManage/Mxkcsq/` |
| 补修课程申请 | `CJManage/BXKCManage/SQBXKCManage/` |
| 学生网上评教 | `JxcpManage/Xscp/XscpManage/` |
| 我的导师信息 | `OneInfoManage/StudentDaoshiInfo/MyDaoshiInfo/` |
| 我的学期规划 | `OneInfoManage/StudentDaoshiInfo/MyXqPlan/` |
| 大创项目申报 | `Reporter/GoIframe/CxCyDs/` |
| 双击位缴费 | `SxwManage/JfMdManage/SxwJf/` |
| 个人消息中心 | `WebIM/MessageManage/MsgControl/` |

---

## 6. WebVPN 通道重写规则

```
https://webvpnnew.jxau.edu.cn/https/{加密串}/原始路径?{vpn 参数}
```

| 目标 | 加密串 |
|---|---|
| `cas.jxau.edu.cn` | `77726476706e69737468656265737421f3f652d22d286945300d8db9d6562d` |
| `jwgl.jxau.edu.cn` | `77726476706e69737468656265737421fae04690693a70516b468ca88d1b203b` |

`vpn` 参数形如 `vpn-12-o2-jwgl.jxau.edu.cn`（域名里的点替换规则未完整验证）。
WebVPN 通道的会话 Cookie 名是 `wengine_vpn_ticketwebvpnnew_jxau_edu_cn`（**带 `new`**）。

> ❗ **WebVPN 通道的协议登录未经实测**。`SiteProfiles.WEBVPN.protocolLoginVerified = false`
> 是诚实标注，不是保守估计 —— 脚本在 WebVPN 模式下走的是 Playwright 浏览器。

---

## 7. 仍然未知 / 需要后续补

| 项 | 状态 | 备注 |
|---|---|---|
| WebVPN 协议登录是否可行 | ❓ | 需要专门一轮验证；失败则回落 WebView 取票 |
| `pcid`（选课批次 ID）的可靠来源 | ❓ | 脚本从 `Xkpc` 取，但窗口未开放时拿不到真实值 |
| 退选是否受时间窗限制 | ❓ | 写操作，窗口开放前无法安全验证 |
| 教室课表 `GetClassroomKebiaoByXq` | 未验 | 猜测路径，未实测 |
| 各接口的 `Result:false` 具体含义 | 部分 | 至少知道 `CheckGuid` 的 false = 未开放 |
| 是否有独立的"已选课程"退选页 | ❓ | 退选接口已找到（`DelXkinfo`），但 UI 入口未确认 |

---

## 8. 复现与证据

- 会话与请求：`tools/probe_pages.py`
- 原始响应落盘：`tools/out/pages/`（已 gitignore，含个人数据不入库）
- 主页面 HTML：`tools/out/menu/menu/00_main.html`
- 会话 Cookie 缓存：`.secrets/jxau_cookie.txt`（已 gitignore）

所有 `✅` 标记的接口都是在「真实登录 + 正确 Referer」下拿到真实数据后才标记的，
**没有一条是"看起来应该能用"**。
