# UI 改造真机验收清单（P0~P3 + 动效 + 切页字符粘连修复）

> ⚠️ **归属声明（2026-09-23 追加）**：本文档属于「去抢课」精简分支
> （`D:\IO\Android\jxautools`）的存档，是从 Pro 版派生时带过来的**原文**。
> 本分支已整体移除选课 / 抢课 / 本地 mock 演练（相关页面、服务、权限、闹钟、
> `Channel.MOCK` 与 debug 明文流量许可都已删除，`applicationId` 也换成了
> `cn.edu.jxau.tools.lite`）。**下文中凡提到这些功能之处，在本分支都不存在**，
> 请当作**对 Pro 版的记述**来读。Pro 版在 `D:\IO\Android\jxautools Pro`（含抢课，是主线）。
> 本次删改的清单与判据见 `docs/改名与去抢课分支实施大纲.md`。
> **为什么要有这份文件**：这几批改动「离线能证的部分」已经证完了 —— 编译零 `e:` 零 `w:`、
> 纯逻辑自检 263/263、静态对账 37/37、变异探针 27/27。但**这些全都证明不了「屏幕上是对的」**：
> 配色成不成套、行高有没有变、点击热区在不在整行上、顶栏滚起来到底收不收，
> 只有真机能判 —— 尤其最后一条，静态断言只能证明「接线写了」，证明不了「真的会折叠」。
>
> 组织方式：每条都是 **操作 → 期望 → 不通过说明什么**。不通过时直接报**编号**给我，
> 定位写在每条的最后一栏，不用你再描述现象。
>
> 提交对应：P0 = `75c39ae` · P1 = `948bc34` · P2 = `d20feef` · P3 = `60d1aea` · 动效 = `1d999d9` ·
> 切页字符粘连修复 = `fff2bf8`。
> 大纲见 `docs/UI改造实施大纲.md`。
>
> ✅ **§1（P0）/ §2（P1）/ §3（P2）/ §4（P3）/ §5（动效）均已于 2026-09-22 在 MuMu 预跑**，
> 每节标题下都附了「实测判据 + 结论」表，判据都是可复算的硬指标，你能直接复核；
> 也仍然建议你照各条自己走一遍 —— 章节里的「操作 / 期望 / 不通过说明什么」保持原样。
> **唯一没跑的是登录页相关的两条**（2-1 与 2-3①）：看登录页必须退出登录，而退出后可能要求
> 人工输入验证码，风险不对等，预跑时没有动登录状态。这两条静态断言已覆盖，真机留给你。
>
> ⚠️ **§5 的预跑结论产生于 2026-09-23 之前**，而那天把过渡结构整个换了（交叉淡入 170/200/260ms →
> 错开结构 300ms）。所以 **§5 里凡是涉及"过渡长什么样"的判据（5-1 / 5-3）都已失效**，
> 需要按 §6 重跑；**5-2 / 5-4 / 5-5 不受影响**（它们测的是状态留存、高度动画、静态断言，与 alpha 结构无关）。

---

## 0. 前置（**先做这两步，否则后面的结论都不作数**）

**0-1 确认设备上装的是含 P1+P2+P3 的包**（末批为 2026-09-23 的切页字符粘连修复，`fff2bf8`）
—— 别用「装上了」当通过。
装机方式沿用你原来的离线构建（`--offline --no-daemon`，`JAVA_HOME=D:/IO/jdk17`）。

> ⚠️ **先确认手里那个 APK 是新打的**（2026-09-22 在这一条上栽过一次）：
> `:app:compileDebugKotlin` **不产出 APK**，它只回答「源码能不能编过」。
> 改了源码却只跑 compile，`app/build/outputs/apk/debug/app-debug.apk` 会一直停在旧时间戳上 ——
> 装上去看到的还是旧界面，而**所有静态验证全绿、编译也确实零 `e:` 零 `w:`**，查无可查。
> 装包前先核这两行的先后：
>
> ```bash
> ls -la app/build/outputs/apk/debug/app-debug.apk    # APK 是什么时候打的
> git log -1 --format='%ci %H'                        # 最后一次改动是什么时候
> ```
>
> APK 早于改动 → 跑 `:app:assembleDebug`（而不是 compile）。
> ⚠️ **本仓库没有 `gradlew`**（无 `gradle/` wrapper 目录），要用本机那份发行版（全量约 80s）：
>
> ```bash
> JAVA_HOME="D:/IO/jdk17" "C:/Users/23836/.gradle/wrapper/dists/gradle-8.10.2-bin/e0thjr3we83usdufs66z371ne/gradle-8.10.2/bin/gradle.bat" \
>   --offline --no-daemon --console=plain :app:assembleDebug
> ```
>
> 还有一个**一眼可辨的判据**：装完看自检的**项数** —— 项数会随自检用例增减，只能同批对比：
> 旧包 `520/520`、含 P3 的包 `654/654`（2026-09-22 实测）。**项数比上一版少 = 装了旧包**
> （同一台设备上旧包 22:22 报 520、新包 22:25 报 654）。
> 2026-09-23 的粘连修复只改过渡结构、不动自检用例，**预期仍是 `654/654`**（这条是推断不是实测：
> 本轮只动 `Motion.kt` 与静态脚本，没加删自检用例 —— 跑出来若不是 654，请告诉我）。

装完进 **我的 → 诊断与日志**，看日志里这一行：

```
=== 全部自检：X/Y 通过 ===
```

判定：**X 必须等于 Y**（0 失败）。数量对不上或出现 `[E]` 行 → 装的是旧包或构建有问题，后面别继续。

**0-2 截图统一走这个命令**（它会自动重连 adb，避免 `device not found`）：

```bash
bash tools/mumu.sh shot p1_login_light      # 存到 tools/out/p1_login_light.png
```

截图放在 `tools/out/`（已 gitignore）。下面凡写「截图」的都指这条命令。

**0-3 底部导航栏别按 dump 给的坐标中心点**（2026-09-22 实测的坑，与 App 无关）：
`uiautomator dump` 报的 `'我的' (793,1552)` 把系统 insets 算进了 bounds，落点其实在
导航栏**下方**的空白区 —— 导航栏真正绘制在 **y=1440..1526**。点 1552 会**完全没反应**，
看着像「子页里切不了 Tab」，其实一条都点不中。按 **y≈1483** 点：

```bash
adb -s 127.0.0.1:7555 shell input tap 335 1483     # 选课 Tab
```

实测：在「诊断与日志」子页点 `(335,1483)` 立刻切到选课页，说明**子页里底部导航是好用的**
（子页属于「我的」，所以「我的」保持选中态是正常的）。

---

## 1. P0 —— 课表 2dp 档位 · 12 主题 + 自定义色相 · 字号缩放

> ✅ **本节已于 2026-09-22 在 MuMu（Android 12，900×1600，自检 `654/654`）预跑一遍，6 条全过**。
> 判据都是可复算的硬指标；你也可以照下面各条自己走一遍。**新增工具** `tools/scan_theme_apply.py`。
>
> | 条 | 实测判据 | 结论 |
> |---|---|---|
> | 1-1 | **浅色 12 张 + 深色 12 张**跨主题扫描（`scan_theme_apply.py`：找「跨主题完全不变 且 有饱和度」的像素）。命中的只有 ① 色块本体 ② 自定义色相卡（预览圆/色相带/滑块 —— 它们显示的是**自定义色**而非主题色，本就该固定）。**底部导航未被标记 → 跟随主题**。另用玫红↔经典蓝在成绩页/选课页对比，命中全是 error 色系（不及格卡片、统计卡、「当前大概率选不了」警告卡） | ✅ |
> | 1-2 | 点色相带 x=700→「**290°**」、x=200→「**60°**」实时变；饱和度三档文字跟着变（`173° · 浓郁 / 柔和 / 标准`）；点「使用」前主界面不动、点后「当前状态」由「经典蓝」变「**自定义（色相 173° · 标准）**」；成绩 Tab 指示器 `#0A305C`→`#093732`（蓝→青绿）；重进页面滑块停在原位、预览圆带 **✓** 且文字为「**当前使用中**」 | ✅ |
> | 1-3 | 界面文字：4 档下「当前状态」卡 y=**1214 / 1320 / 1428**（逐档下移 106 / 108px），特大档被推出屏幕。课表：**特大 vs 小 做偏移扫描 → dy=+9 时差异 `0.000%`**（课表逐像素完全一致，只有整体平移 9px，来自顶部界面文字变高） | ✅ |
> | 1-4 | 三档两两差异 **14.2% / 14.0% / 17.3%**；**等宽档真生效**（中文字距变宽、示例行折成两行）；衬线档中文回落黑体（MuMu 缺中文衬线字面，属预期）但标点形状变了 | ✅ |
> | 1-5 | 两端都设到（40dp/48dp 与 100dp/104dp，课名字号 **10sp→15sp** 连续变化）。`geometry` 行距 **43.0dp / 103.0dp**（= 档位+3dp 间隙）；`columns` @48dp **5 列全对齐、栅格残差 0.0px**；`block_fit` @104dp **漏出像素 `[0]`**；`stripe_contrast` 深色 1.0567/1.1790、奇偶交替、描边 1.5708 全 PASS；横滚到右端后列距仍 **214px = 107.0dp**、最右列右边界 **x≈891 < 900**（完整可见） | ✅ |
> | 1-6 | force-stop 重启：浅色·竹青 / 字号大 / 格子 72dp·列宽 64dp **全部保留**；自检 654/654；crash buffer 清空后重启**崩溃 0** | ✅ |
>
> ⚠️ 预跑中改过的设置**已全部改回**（浅色·竹青 / 字号大 / 字族默认 / 72dp·64dp / `custom_hue=210`），
> 核对方式：`adb shell run-as cn.edu.jxau.tools cat shared_prefs/jxau_settings.xml`。

### 1-1 十二个主题色都真的生效（找「还是紫色的」漏网角色）★ 最重要

**操作**：我的 → **外观主题** → 「配色模式」切**浅色** → 「**主题色**」卡里依次点 **12 个色块**
（6 列 × 2 行：绯红/暖橙/琥珀/橄榄/草绿/竹青/翡翠/青碧/经典蓝/紫罗兰/品红/玫红），
每点一个截一张；再把「配色模式」切**深色**，重来一遍（共 24 张）。

**期望**：每次切完，**整屏**都跟着变 —— 特别是这四个最容易漏的地方：
底部导航栏底色与选中态、卡片标题与图标、`FilterChip` 与选中态按钮、进度条与开关轨道。

**不通过说明什么**：任何一个部件还是紫色 → 该处**没走 `MaterialTheme.colorScheme`，
是硬编码或 M3 baseline 泄漏**。这正是上一轮「底部导航一直淡紫」那类缺陷的同类。
反馈请报「色块名 + 浅/深 + 哪个部件」，并附截图。

### 1-2 自定义色相滑块

**操作**：我的 → 外观主题 → 「**自定义色相**」卡 → ① 点色相带上任意一段；② 拖下方滑块；
③ 换三档饱和度（柔和/标准/浓郁）；④ 最后点右侧「**使用**」。

**期望**：
- 拖动/点选时，卡片上方那个**预览圆**与「色相 xxx° · 档位」文字**实时**跟着变；
- 点「使用」之后是**整个界面**（含底部导航）才跟着变 —— 启用是**显式**的，不是拖完自动生效
  （防止用户拖的过程中界面乱闪）；
- 重新进这个页面时，滑块停在你上次的位置，且预览圆上带 ✓ 表示正在使用中。

**不通过说明什么**：拖滑块时预览圆也不动 → `rolesForHue` 实时预览链路断了；
点「使用」后界面不变 → `customAccent` 没传进派生链路（会回落成默认浅蓝，
界面表现为「选了没反应」）；重进页面回跳 → 落盘的不是最新值。

### 1-3 字号 4 档：界面文字变、**课表字号必须不变**

**操作**：我的 → **字体** → 「**字号缩放**」→ 依次选 小 / 标准 / 大 / 特大，每次回课表页看一眼。

**期望**：界面文字（标题、列表、按钮、以及同页「预览」卡）跟着缩放；
**课表格子里的课程名字号不动**。

**不通过说明什么**：课表字号也变了 → 有人把全局缩放接到了 `LocalDensity.fontScale`
（会撞「字撑破格子」）。这是**刻意写成规格断言**的一条：课表字号由列宽推导，不跟全局缩放。

### 1-4 字族 3 档

**操作**：我的 → **字体** → 「**字族**」→ 默认 / 衬线 / 等宽。

**期望**：「默认」是黑体；衬线/等宽**若系统支持**则中文变字形。**中文回落黑体不是 bug**
（部分 ROM 没有中文衬线/等宽字面），页面文案已经写了「若系统支持」。

**不通过说明什么**：三档完全无差别**且**英文/数字也没变 → 字族没接上；只有中文没变 = 正常。

### 1-5 课表尺寸两端档位（列宽 48dp / 104dp，行高 40dp / 100dp）

**操作**：我的 → **课表显示** → 「**格子尺寸**」→ 把行高和列宽都拖到**最小**（页面上有实时预览），
回课表页截图；再拖到**最大**，截图。

**期望**：横向能平滑滚到最左/最右，最右一列**完整可见**（不是被切一半）；
表头周几与下面的课程列**始终对齐**（滚到两端也对齐）；行高变化时底纹格与课块**不留缝**。

**不通过说明什么**：末端列错开 → 表头与网格两条渲染路径的**总宽**算得不一致；
块下方露一条背景 → 课块可见矩形与底纹格不等（`CELL_INSET_DP` 那条对齐关系）。

**顺带**：这 4 张截图正好喂给 4 个像素脚本（**脚本不用改，截图要重截**）：

```bash
python tools/measure_timetable_geometry.py tools/out/p0_w_min.png tools/out/p0_w_max.png
python tools/measure_timetable_columns.py  tools/out/p0_w_min.png --colw=48
python tools/measure_block_fit.py          tools/out/p0_w_max.png
python tools/measure_stripe_contrast.py    tools/out/p0_tt_light.png tools/out/p0_tt_dark.png
```

> 它们的期望值来自 `TimetableSurface.kt`（本轮未动），所以**档位加密不影响脚本**；
> 影响的是**截图坐标基准**，所以要重截。这 4 个脚本对本轮改动的判别力有限，
> 不能拿它们当验收 —— 它们的用途是「回归：尺寸改了但渲染没跟上」。

### 1-6 杀进程重启后设置仍在

**操作**：把设置改成非默认（比如主题「翡翠」+ 字号「大」+ 行高 82dp）→ 从最近任务划掉 →
重新打开。

**期望**：三项都还在。

**不通过说明什么**：回默认值 → 落盘失败或读取时键名/默认值不匹配。

---

## 2. P1 —— 控件归位

> ✅ **本节已于 2026-09-22 在 MuMu（Android 12，900×1600，自检 `654/654`）预跑，6 条里 4 条通过、
> 2 条卡在登录页（见下）**。
>
> | 条 | 实测判据 | 结论 |
> |---|---|---|
> | 2-1 | **未验** —— 看登录页必须退出登录，而退出后可能要求人工输入验证码，风险不对等，我没动登录状态。静态断言已覆盖：`verify_ui_controls.py` §3 查「`SegmentedButton` 4 处 / `RadioButton` 0 处」 | ⚠️ **留给你** |
> | 2-2 | 三段连通胶囊（跟随系统 / **✓ 浅色** / 深色），选中段有填充 + ✓；说明文字在下方**单独一行**且跟着换（「始终使用浅色界面」→「始终使用深色界面」） | ✅ |
> | 2-3 | ② 学籍信息隐私开关：点**文字**(x=390) / **中间空白**(x=650) / **开关**(x=820) —— **三处都能切换**，两种文案互换（「身份证号、考生号、家庭住址、邮编已默认遮蔽。」↔「身份证号、住址等已完整显示。这些内容不会写进日志，也不会被导出。」），字段展开随之变（「基本信息」y 343→353→343）。① 登录页「记住密码」**未验**（同 2-1） | ✅ / ⚠️ |
> | 2-4 | 成绩选中 = **实心 ★ + 胶囊**，课表/选课/我的 = **描边**；切到选课后成绩立刻变**描边 ☆**、选课的 ⊕ 变实心 | ✅ |
> | 2-5 | 指示器 **y=266..271（高 6px = 3dp）**、**x=192..257（宽 66px）**、**两端收窄（64 vs 66）→ 圆角胶囊**；tab 宽 450px，指示器仅其 **1/7** → **不是 M2 的全宽下划线** | ✅ |
> | 2-6 | 「不及格」标签：粉红底 + 深红字，清晰可读（同页那张「3 门不及格」统计卡也一样） | ✅ |

### 2-1 登录页「访问通道」是三段，不是三个圆点

**操作**：登录页第一张卡。

**期望**：一行三个**连通的分段**（自动 / 直连 / WebVPN），选中的那段有填充色，
整段可点；**点哪段立即生效**（不用点「确定」）。

**不通过说明什么**：还是三个 RadioButton 圆点 → 装的是旧包（回 0-1 重查）。

### 2-2 外观主题页「配色模式」也是分段（不是竖排单选）

**操作**：我的 → 外观主题 → 配色模式。

**期望**：系统 / 浅色 / 深色 三段一行；**当前项的说明文字在下面单独一行**，
切段时说明文字跟着换。切到「浅色/深色」应立即改变整个界面。

### 2-3 两个开关：**点整行任意位置都能切** ★ 容易只测到一半

**操作**：
- ① 登录页「记住密码」那一行 —— 点文字、点开关、点**两者之间的空白**；
- ② 我的 → 我的信息 → **学籍信息** → 页面**顶部**那条隐私提示（正文是「身份证号、考生号、
  家庭住址、邮编已默认遮蔽。」），同样三处都点。

**期望**：三处都能切换；② 切换后下面字段跟着展开/遮蔽，且提示文案本身也换。

**不通过说明什么**：只有点开关本体才响应 → 整行 `toggleable` 丢了，
只剩 `Switch` 自己的小热区（这是「能编译、界面正常、行为退化」的典型）。
另外 ② 的 `Switch` 必须传 `onCheckedChange = null`，否则开关和整行会各接一次点击 → **一下点两回**（切了又切回去）。

### 2-4 底部导航：选中态**换形状**，不只换颜色

**操作**：四个 Tab 来回切。

**期望**：未选中是**描边**（outlined）图标，选中是**实心**（filled）图标。

**不通过说明什么**：只有颜色变、形状不变 → 图标配对丢了。
这条对色觉障碍用户有实际意义，所以单独验收。

### 2-5 选课页内层切换是 pill 指示器

**操作**：选课页 → 「课程 / 抢课任务」。

**期望**：选中项底下一个**胶囊形**指示器（M3 `PrimaryTabRow`），不是细下划线。

### 2-6 成绩页标签仍然**清晰可读**（这处是刻意不统一的）

**操作**：成绩页，找带标签的行（不及格 / 补考）。

**期望**：标签文字**读得出来**（半透明底色 + 深色字的组合）。

**不通过说明什么**：出现「深底深字」→ 容器色/内容色配错。这类错的可怕之处是
**两个颜色各自合法**，编译器和运行时自检都不报 —— 这也是 `verify_ui_controls.py` 存在的原因。

---

## 3. P2 —— 设置页行归位（M3 `ListItem`）

> ✅ **本节已于 2026-09-22 在 MuMu（Android 12，900×1600，自检 `654/654`）预跑一遍，3 条全过**。
>
> | 条 | 实测判据 | 结论 |
> |---|---|---|
> | 3-1 | 像素采样三层色（「考试安排」/「学籍信息」/「外观主题」三行**完全一致**）：图标 `#2C7930`（**sat=77**，竹青主色）/ 标题 `#1A1C1E`（sat=4）/ 摘要 `#44474F`（sat=11）—— 三层分明，图标不是灰的 | ✅ |
> | 3-2 | 「考试安排」行测 **5 个落点全部进子页**：图标 x=32/56 · 标题 x=201 · 箭头正上方 x=828 · 卡片内最右 x=860/866。**x=872 不响应是对的** —— 像素量出卡片右边界正是 **x=868**（内 `#FFFFFF` / 外 `#F8F9FC`），872 已在卡片外 | ✅ |
> | 3-3 | 分组标题左边缘与卡片内图标左边缘**对齐**；行距一致 **146px**；「分组标题→首行」间距 **86px**（三处一致）；行高约 **70dp**（≈ M3 两行 72dp）；行间分隔线可见。⚠️ 无 P2 前截图，只能核「当前是否符合大纲描述」，无法做前后对照 | ✅ |

### 3-1 首页四个分组的入口行：颜色分三层，不是一层灰 ★ 最重要

**操作**：我的（首页），从上往下看「我的信息 / 设置 / 会话与维护 / 其他」四组、共 **12 行**。

**期望**：
- 左侧图标是**主色**（不是灰的）；
- 标题是正常前景色、**加粗适中**；
- 下面那行摘要是**次要色**（比标题淡，但比「禁用」明显）。

**不通过说明什么**：图标和摘要塌成同一个灰色 → `ListItem` 的槽位配色没显式给
（它的默认值全走 `onSurfaceVariant`）。三层色是 Pixel 设置页的形态，也是「一眼看清主次」的底线。

### 3-2 点**整行任意位置**都能进子页（含最右边箭头那一带）★ 最容易漏

**操作**：任取一行，分别点 ① 图标 ② 标题文字 ③ 右侧箭头 **正上方/正中间** ④ 最右边缘空白。

**期望**：四处都能进对应子页。

**不通过说明什么**：只有文字那一块能点、箭头浮在行中间 → `ListItem` 漏了 `fillMaxWidth()`
（它内部**不撑满宽度**）。这个缺陷我是在写进代码前自己抓到的，
`verify_ui_controls.py` §5 和探针各有一条守着它 —— 但**只有真机能证明守住了**。

### 3-3 行高与之前一致 + 两处**有意**的变化

**操作**：对比 P2 前后（或凭印象）。

**期望**：
- **行高基本不变**（原来手算 60dp，M3 两行 `ListItem` 也是 60dp）；
- **左右内边距从 14dp 变 16dp**（略宽一点，这是有意的）；
- **分组标题与卡片内的图标左对齐**（标题原来偏左 4dp，现在 16dp）；
- **行间分隔线比之前略明显**（`outline@15%` → M3 默认 `HorizontalDivider`，有意）。

**不通过说明什么**：行高明显变高/变矮 → 摘要为空的那几行被当成了两行排版
（`supportingContent` 应该是 `null` 而不是空 lambda）。

---

## 4. P3 —— 顶栏（「我的 / 成绩 / 选课」加，课表页刻意不加）

> 这一节的**全部结论都只能靠真机**：静态断言能证明「`nestedScroll` 接线写了」，
> 但证明不了「顶栏真的会折叠」—— 那要 M3 的滚动竞态真的跑起来。

> ✅ **本节已于 2026-09-22 在 MuMu（Android 12，900×1600）用新包**（自检 `654/654`）**预跑一遍，
> 5 条全过**。判据都是可复算的硬指标，你能直接复核；也可以照下面各条自己走一遍：
>
> | 条 | 实测判据 | 结论 |
> |---|---|---|
> | 4-1 | 课表页顶部仍是 `'2026-2027 第1学期'`(y=92)，**无标题栏**；轴 pitch 7 个间距**全 `134.0px`**、列距 3 个**全 `154.0px`**（离散度 0）；轴首行中心 `y=392`（uiautomator 与像素两种手段互证）；4 个像素脚本全 PASS，含 `漏出像素数分布 [0]`、表头/网格栅格残差全 `0.0px` | ✅ |
> | 4-2 | 「成绩」顶栏 `'成绩'`(76,112)；下滑后该标题**从 dump 里消失**、最顶内容 `'2025-2026 第1学期'` 落到 **y=56**（≈状态栏高度，顶栏高度归 0）；只回滑 160px 标题**立刻**回到 y=104 | ✅ |
> | 4-3 | y=0..199 两条竖列（x=20 / x=880）**逐像素同色 `#F8F9FC`** —— 状态栏与顶栏无色差、无空白 | ✅ |
> | 4-4 | 学籍信息 / 外观主题 / 诊断与日志三个子页均为 `'标题'(·,112)` + `'返回'(56,112)`；滚动两屏后两者坐标**一动没动** | ✅ |
> | 4-5 | `=== 全部自检：654/654 通过 ===`；`[E]` 零条；`FATAL EXCEPTION` 零条 | ✅ |

### 4-1 课表页**必须没有**顶栏，且高度与之前一模一样 ★ 最关键的一条

**操作**：切到课表 Tab，对比 P3 之前（或与截图）——看最上面是不是「学期选择 + 回到本周」，
课表格子区从原来的位置开始。

**期望**：**课表页顶层没有任何新增的标题栏**；11 节的滚动高度、表头位置、格子尺寸全部不变。

**不通过说明什么**：多出一条「课表」标题栏 → 有人在 `AppRoot` 里加了统一的 `topBar`
（那样四个 Tab 会一起加），或直接给 `TimetableScreen` 加了 —— **这个取舍是你拍板定的
（课表要竖着滚 11 节，不让出 64dp）**，静态断言 §6 有两条守着，真机再确认一次。
另外：如果课表区整体下移了 64dp，那 4 个像素脚本的截图基准就得重标（本来不用）。

### 4-2 三个主页的顶栏 + 折叠 ★ 核心行为

**操作**：分别进「我的」「成绩」「选课」，各做一次：**向下滑到底 → 再往回滑一点 → 停在中间**。

**期望**：
- 顶栏标题分别是 **我的 / 成绩 / 选课**（不是串了别的页的标题）；
- **向下滑：顶栏收起并完全让出高度**（内容顶到状态栏下面）；
- **往回滑一点：顶栏立刻收回来**（`enterAlways` 的行为，不用滑到顶）。

**不通过说明什么**：
- 顶栏**一直不动** → 折叠接线漏了/断了。这是最典型的静默失效：不报错、界面正常、特性等于没有。
  静态断言 §6 有一条查「三个主页都把折叠接到了页面根容器」，探针里另有两条变异守它；
- 顶栏收起来了但**内容没跟着上移**（留一条 64dp 空白）→ 折叠没让出布局高度；
- 切 Tab 之后发现**新页面的顶栏是收着的** → 三个页面共用了同一个折叠状态（应该各自 `remember`）。

### 4-3 顶栏与状态栏之间**不能**出现一条空白或色带

**操作**：三个主页各看一眼顶部（浅色主题最容易看出色带）。

**期望**：状态栏那一条的颜色与顶栏**同一个颜色**，顶栏标题紧贴状态栏下方（不留额外空白）。

**不通过说明什么**：
- 顶栏上方多出一条空白（约一个状态栏高）→ 顶栏又吃了一次系统内边距（`windowInsets` 没置 0）；
- 状态栏那一条与顶栏**颜色差一档**（一条横向色带）→ 顶栏容器色没跟 `Scaffold` 对齐
  （`surface` vs `background`）。

### 4-4 子页：顶栏是 M3 的，且**返回按钮不会滑走**

**操作**：我的 → 学籍信息（或任选一个子页）：
① 看顶栏形态；② **把内容往下滑**，看返回按钮在不在。

**期望**：
- 顶栏是 M3 的标题栏样式：标题比之前**大**（M3 顶栏标题是 22sp，原来是 16sp）、
  整条高 64dp（原来是 54dp）—— 这是**有意**的，就是「要像 Pixel」的那部分；
- 左上角返回按钮一直都在（子页顶栏**固定不动**），点它能返回；
- 「我的」页首页与子页**不会同时出现两条标题栏**。

**不通过说明什么**：出现两条标题栏 → 「我的」页 Hub 的顶栏没有随子页切换而让位；
返回按钮会滑走 → 子页顶栏错接了折叠行为（用户得先往回滚才能退出，这是最不该藏的控件）。

### 4-5 十二个子页都还在（顺手扫一遍）

**操作**：诊断与日志 → 看自检 `=== 全部自检：X/Y 通过 ===`，**X 必须等于 Y**；
再随手进两个子页确认都有标题栏与返回按钮。

**期望**：自检全绿；每个子页都有标题栏 + 可用的返回。

**不通过说明什么**：某个子页没有标题栏/返回按钮 → 它没走 `DetailScaffold`。
静态断言查了「12 处调用点仍在」，但真机扫一眼更直接。

---

## 5. 动效（提交 `1d999d9`：Tab 切换 / 子页进出 / 三态互换 / 列表项）

> ⚠️ **这张表的预跑发生在 2026-09-23 改结构之前**（当时是交叉淡入 170/200/260ms）。
> **5-1 / 5-3 的判据已失效**（"过渡长什么样"变了）→ 按 **§6** 重跑；
> **5-2 / 5-4 / 5-5 不受影响**（分别测状态留存、高度动画、静态断言，与 alpha 结构无关），
> 但 5-5 行内引用的计数是旧的（当时 17/17 + 14/14，现为 31/31 + 20/20）。

> ✅ **预跑记录（2026-09-22，MuMu）**：5/5 过。判据与证据：

| # | 操作 | 期望 | 实测判据 | 结论 |
|---|---|---|---|---|
| 5-1 | 切 Tab 看过渡 | 有方向感横滑，不是硬切 | 连拍 8 张，`measure_motion_transition.py` 抓到 1 张既非前页也非后页的中间态（dA 3.02% / dB 47.03%，稳定帧噪声 0.02~0.48%）；再装**慢速变异包**（时长 ×8）连拍，两个方向各 5 张中间态、dB 单调递减 60.40%→26.40%→21.26%→14.47%→4.00%→0.00% | ✓ |
| 5-2 | 成绩页滚动后切走再切回 | 位置保留，回到原处 | 滚到「中国近现代史纲要」→ 切选课 → 切回，首条可见项逐项一致 | ✓ |
| 5-3 | 子页进出 | 有过渡，返回正常 | 「我的 → 各子页」进出流畅，返回键与返回按钮都正常 | ✓ |
| 5-4 | 动画期间稳定性 | 无崩溃 | `logcat -b crash` 过滤本包名 = **0** | ✓ |
| 5-5 | 列表项动画的前提 | key + motionItem | 静态断言 `verify_motion.py` 17/17 PASS、变异探针 14/14 CAUGHT（源码指纹 md5 复核一致）——**这是当时的计数；2026-09-23 后为 31/31 + 20/20** | ✓ |

### 5-1 Tab 切换有方向感的过渡 ★
**操作**：来回切 4 个底部 Tab。
**期望**：向右切（更高序号的 Tab）内容从右侧推进来，向左切反之；时长短促（≤300ms）不拖沓。
**不通过说明什么**：硬切无过渡 → `AppRoot` 里 `MotionPager` 没接上；方向反了 → ordinal 判断写反。

### 5-2 切走再切回，滚动位置保留 ★ 容易只测到一半
**操作**：成绩页滚到中间 → 切到选课 → 切回。
**期望**：还在刚才那一条附近。
**不通过说明什么**：回到顶部 = `SaveableStateProvider` 掉了。**这一点已做变异实验实锤**：
把 `SaveableStateProvider` 摘掉重装，切回后回到顶部（「已获学分 41.5」表头），位置确实丢失——
即 5-2 的行为**确实由它带来**，不是碰巧。选课页两半、我的子页同理。

### 5-3 页内三态切换有过渡
**操作**：找一个会失败的请求（或断网）触发「加载中 → 失败」。
**期望**：状态间交叉淡入，且**不会看到两个旋转相位不同的转圈叠在一起**（Idle 已并入 Loading）。
**不通过说明什么**：叠转圈 = `motionPhase` 合并丢了；硬切 = `MotionSwap` 没接上。

### 5-4 登录页运行日志卡展开/折叠
**操作**：登录页点开运行日志再收起。
**期望**：高度平滑变化，不是瞬间出现/消失。
**不通过说明什么**：瞬间 = `motionHeight` 没接上。

### 5-5 列表项增删有位移动画
**操作**：选课页对一门课「选上 / 退掉」，看它进列表 / 出列表。
**期望**：相邻项平滑让位，不是整列跳变。
**不通过说明什么**：跳变但无动画、也不崩 = 八成是列表 `key` 掉了（`motionItem` 没 key 时等于没写，
静态断言 §5 已把「key 的值」钉住，`key = null` 也算没写）。

---

## 6. 切页字符粘连修复（提交 `fff2bf8`，2026-09-23）

> **这次改的是结构，不是时长**：`MotionPager` / `MotionSwap` 的进出 alpha 从「同起同止（交叉淡入）」
> 改成**错开**（出 `fadeOut(90)`、进 `fadeIn(210, delayMillis = 90)` —— M3 SharedAxisX 官方数值），
> 位移两侧都 300ms、距离固定 30dp（原来是半屏）；每层内容补了不透明底；两处容器加了 `clipToBounds()`。
> 改动全在 `ui/Motion.kt`，**11 个调用点一行未动**。
>
> **离线已证的部分**（可复算）：`verify_motion.py` 31/31 PASS · `probe_motion.py` 20 条变异 20 条 CAUGHT ·
> 编译零 `e:` 零 `w:` · APK 时间戳晚于源码。**这些只证明「结构写对了」，
> 证明不了「肉眼看不出残留」** —— 剩下几条只有真机能判。

### 6-1 定性：控件**之间的空隙**里不该再有旧页的字 ★ 先跑这条
**操作**：装新包，在「我的」页反复进/出子页（首页 ↔ 学籍）、来回切 4 个底部 Tab，各做 5 次。
**期望**：过渡的**前半段**不再出现任何旧页文字。
**怎么看**：⚠️ 顶栏与各类 `Card` 本来就自带不透明底、会把旧字盖死 —— 那是修复前后都成立的
（诊断里叫「只有自带容器色的部件才盖得干净」）。要看的是**控件与控件之间的空隙**：
列表行之间、标题与正文之间、纯文字区域。
**不通过说明什么**：空隙里仍有字 → 两个 alpha 窗口还在重叠。查 `enterFadeSpec()` 的
`delayMillis` 是不是掉了，或退出时长被调大到超过 90ms。

### 6-2 硬判据：旧页字形与新页字形**不得同时可见**（慢速变异包）★ 唯一直接证据
**操作**：把 `EnterFadeDelayMillis` / `EnterFadeMillis` / `ExitFadeMillis` / `SlideMillis`
**按比例 ×8**（90→720、210→1680、300→2400）重装，连拍过渡中间帧；每个方向各取 ≥5 张。
**期望**：每一张中间帧里，「旧页独有文案」与「新页独有文案」的像素集合**不同时非空**。
**文案怎么选**：必须**两页各自独有**。例：切到选课时，前页（成绩页「已获学分」）与后页
（选课页「课程 / 抢课任务」）各取一处只在那一页出现的字。
**不通过说明什么**：两者同时出现 = 结构又退回重叠了。
**⚠️ 跑完必须还原那几个常量并重建正式包** —— ×8 会让 `verify_motion.py` §2b（每条 ≤300ms）报 FAIL，
那是**预期**的，别当成缺陷。配套量化脚本 `tools/measure_motion_transition.py`。

### 6-3 别把正常语义误判成残留（这条是防误报）
**操作**：看子页返回（「我的 → 学籍」再返回首页）、看 Tab 往左切。
**期望**：旧页在**还没被滑到的那一块区域**仍然可见 —— 那是**横向滑动的定义**，不是残留。
**判据**：看**被覆盖到的区域**是不是**立刻**不见了；修复前的区别在于那时是**整屏**都在同时显示两层。
**不通过说明什么**：被覆盖的区域还透出旧字 → 是真残留，按 6-1 定位。

### 6-4 顺手复查三条不该被这次改动影响的（回归）
- **5-2 重跑**：成绩页滚到中间 → 切选课 → 切回，位置仍在原处。
  （本次没动 `SaveableStateProvider`，但给 `AnimatedContent` 的 modifier 加了 `clipToBounds()`，复查更稳。）
- **5-4 重跑**：登录页运行日志卡展开 / 折叠仍是平滑高度变化。
- **滚动与手势**：课表页横滑、各列表纵向滚，不应因为 `clipToBounds()` 出现"切边"或滚不动。
  （裁剪的是**过渡容器的边界**，列表滚动本来就在容器内 —— 理论无影响，实测确认一下。）
- **状态栏那一条**：顶栏与状态栏之间仍不应出现空白或色带（见 4-3）。

### 6-5 动画期间稳定性
**操作**：连续切 20 次 Tab / 子页，然后 `adb -s 127.0.0.1:7555 logcat -b crash -d` 过滤本包名。
**期望**：0 条。
**不通过说明什么**：崩了 → 大概率出在 `LocalDensity` 读取或 `clipToBounds()` 的位置，报编号给我。

---

## 7. 报反馈的格式

每条报 **编号 + 现象 + 截图名** 就够，例如：

> 3-2 不通过：点右侧箭头那一带没反应，只有文字能点。截图 `p2_home_row_b.png`

我可以直接从编号定位到源码位置，不用你再描述。
