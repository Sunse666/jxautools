# 江农工具箱（jxautools）项目分析与文档总结

> 生成于 2026-09-24，基于对 `D:\IO\Android\jxautools`（去抢课精简版）的代码勘察，
> 以及 `D:\IO\Android\jxautools Pro\docs\` 下 10 份文档的通读。

---

## 一、项目定位

**江农工具箱**：面向江西农业大学学生的教务系统安卓客户端。原生 Kotlin + Jetpack Compose（Material 3），
登录走学校 CAS 认证（RSA 加密），数据直连教务系统 `jwgl.jxau.edu.cn`，提供课表、成绩、考试安排
（含 .ics 导出）、学籍、导师/学期规划等功能。**纯本机执行、零后端、零第三方服务。**

当前仓库是 **去抢课精简版**（分发版），由 Pro 版派生：

| | Pro 版（自用全功能） | 精简版（本仓库，给同学分发） |
|---|---|---|
| applicationId | `cn.edu.jxau.tools` | `cn.edu.jxau.tools.lite`（2026-09-23 拍板，两版可同机共存） |
| 选课/抢课（RushService 等 11 文件 2365 行） | 有 | 已删除，权限面只剩「联网 + 网络状态」两项 |
| 版本 | — | 0.1.0（versionCode 1） |

---

## 二、技术栈与构建

- **语言/UI**：Kotlin 2.0.21，Compose BOM 2024.12.01，Material 3；minSdk 26 / targetSdk 35 / compileSdk 35，JVM 17
- **网络**：OkHttp 4.12（CookieJar 自实现）+ Jsoup 1.18.3（从教务主页菜单挖接口路径）+ kotlinx-serialization-json 1.6.3（只走运行时 JsonElement API，不启用编译器插件）
- **构建链**：AGP 8.7.3，阿里云 Maven 镜像优先；release 签名凭据放 `.secrets/keystore.properties`（缺失不报错，退化为未签名包），v1/v2/v3 签名全开；**R8 暂未开**（签名与混淆分两段走，见《发布说明》）
- **依赖注入/导航**：都不用——单例 + StateFlow 管 Config/Session，`when(tab)` 管导航，明确拒绝为将来买单

---

## 三、代码结构地图（`cn.edu.jxau.tools`）

```
core/          JxauLog（日志）、SelfTest（纯函数自检汇总入口）
data/
  net/         CasAuth/CasRsa（CAS 登录+RSA）、JwglApi（教务接口）、
               SiteProfiles/MenuProbe（站点画像+菜单探测）、Http、FetchWithHeal（会话自愈）
  model/       课表网格、周次推算（WeekMath/WeekParser/TermAnchor）、学籍、成绩统计、
               导师规划、偏好模型等纯数据层
  export/      ExamIcs / IcsWriter（考试安排导出 .ics，经 FileProvider 分享，零存储权限）
  Session*/Settings*  会话与偏好的单例仓库 + StateFlow
ui/
  login / timetable / grade / exam / profile / advisor / student   各页 Screen + ViewModel
  theme/       12+ 主题色、自定义色相、字体链路、排版
  Motion.kt    全应用过渡动画唯一入口（M3 SharedAxisX「先出后进」）
  AppRoot.kt   登录门禁：持续订阅会话状态，掉线/续期自动切页
```

**JwglApi 三条调用红线**（违反即拿「没有权限」错误页）：一律 POST；必须带 `start`/`limit` 分页参数；带所属页面 Referer。全部端点来自 2026-09-20 实测，逐条见接口清单。

**工程特色**：
- `SelfTest` 启动即跑纯函数自检（RSA 分块、周次解析、课表行归纳等），期望值用 Python 独立算出再抄入，**不是回填实现结果**——防「看着对、边界错」的静默失效
- `tools/` 目录 36 个 Python 脚本：离线构建（`build_lite`）、UI 探针/对账（`probe_*` / `verify_*`）、mock 教务系统（`mock_jwgl.py`）、图标生成等，全链路命令行可跑
- 偏好键存字符串不存枚举序号、配置一律单例 + StateFlow、尺寸读入一律 `snap()` 吸附——全是踩坑后固化的纪律

---

## 四、文档库总结（Pro 版 `docs/`，精简版注释多处引用）

文档按「**分析 → 接口事实 → 功能方案 → 实施记录 → 验收 → 发布/运维**」形成闭环，纪律是**一切结论以实测+对账为据**：

1. **《教务系统分析与安卓App大纲》** —— 项目总纲。基于 4658 行 Python 抢课脚本的静态分析产出整体设计：12 条已确认决策、CAS 登录链路、会话自愈、M0–M3 里程碑。所有文档的源头。
2. **《教务系统接口清单》** —— 最核心的接口事实库（695 行）。实测确认 17+ 可用端点，记录 Referer/GBK 编码/`Result` 不可信/周次锚点反推等坑，含 WebVPN 规则与 mock 契约。持续更新中（2026-09-21）。
3. **《教务系统可扩展功能分析》** —— 功能盘点：主菜单剩余 16 项逐个探到底，实测 14 个新接口，按 T1/T2/T3 给出可做/慎做/不做结论。T1/T2 大纲的直接上游。
4. **《T1功能实施大纲》** —— T1 批次方案+交付记录：导航合并、考试页+.ics 导出、学籍页（隐私脱敏）、导师/规划页。P0/P1 已交付并真机验收（自检 520/520、对账 54/54）。
5. **《T2功能实施大纲》** —— T2 批次方案：「三问」排期法，候选功能含成绩单 PDF 下载、学分进度、找空教室；系统修正 3 处旧接口结论。**方案已就绪、P0 未实施**，多条待实测项待补。
6. **《UI改造实施大纲》** —— UI 改造总方案：M3 风格归位、主题色 6→12+自定义色相、课表尺寸 31/29 档精细化；§10 详述「切页字符粘连」四环根因与修复。P0–P3 已全部落地并附离线验证。
7. **《UI改造真机验收清单》** —— 上述方案的可执行验收手册，每条按「操作→期望→不通过说明什么」组织。§1–§5 已 MuMu 预跑，§5 过渡类判因 09-23 结构变更标失效、需按 §6 重跑。
8. **《发布说明》** —— 签名密钥、release 构建命令、装前卸载 debug 包等发布纪律；界定 Pro/精简版两仓库关系。**段 1（签名+release 包）已完成，段 2（R8+真机冒烟）未开始。**
9. **《工程踩坑总表》** —— 全项目经验教训总账（自 MEMORY.md 拆出）：界面静默失效、课表行高真空隙、M3 紫色泄漏、ICS 导出约束、开机闹钟七条深坑、工作纪律等。持续追加的活文档。
10. **《改名与去抢课分支实施大纲》** —— 拆库方案：改名 Pro + `git clone --local` 派生精简版并删抢课 11 文件。文中标「未执行」，但从发布说明与精简版现状看，**拆分实际已按此完成**（文档状态标注滞后于现实，可考虑补一行「已执行」）。

**引用关系**：大纲→接口清单（取代其旧表）；可扩展功能分析→T1→T2（接口清单为共同事实底座）；UI 大纲↔验收清单互为方案/验收；踩坑总表与 T1/T2/UI 大纲双向印证；发布说明界定两仓库。

---

## 五、当前状态与待办

**已完成**：精简版派生与权限裁剪、签名与 release 构建打通（段 1）、UI 改造 P0–P3、T1 批次交付、808/520 级别的自检与对账体系。

**待办（按文档记录）**：
- 发布段 2：R8 混淆 + 正式包真机冒烟
- T2 批次：P0 功能未实施，接口待实测项待会话登录后补测
- UI 改造 §6 过渡类验收需按新结构重跑
- 《改名与去抢课分支实施大纲》状态标注滞后，建议补「已执行」结论
